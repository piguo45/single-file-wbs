# 計画・課題管理ツール（WBS Viewer） - CLAUDE.md

> English: [`CLAUDE.en.md`](CLAUDE.en.md)（本ファイルが正。**仕様を変更したら ja/en を同じコミットで更新**すること）

テキスト（JSON）駆動の管理ツール。**計画（WBS）と課題（Issue）を 1枚の HTML・1つの JSON で回す。**
データは「**案件（`projects[]`）の中に 計画（`tasks`）と課題（`issues`）が並ぶ**」1つの JSON。
つながりは**課題側の `links[]` にだけ**書き、画面が逆引きして両方向へ1クリックで飛ぶ。

| ファイル | 位置づけ |
|---|---|
| **`wbs_viewer.html`** | **製品の入口。これを開く。**「計画｜課題」の2択スイッチで切り替える（単一HTML・依存ゼロ） |
| `wbs.json` | データの single source of truth。計画も課題もここに入る（ファイル名は自由。慣用名が `wbs.json`） |

**このファイル1本で、計画（`tasks`）も課題（`issues`）も完結する。**
**工数の式・ガント・イナズマ線・進捗の査定・課題の起票とクローズまで、すべてここに書く**（説明書を別ファイルに分けない＝AI が1本読めば済む）。
**計画だけの従来の `wbs.json`（`issues` が無いファイル）もそのまま開ける**（→「後方互換の約束」）。

**読む順の目安**：共通（ビジョン・原則・環境・画面・編集・データ）→ 計画（計算ロジック・追加と更新）→ 課題（導出値・4つの動き・依頼例）→ 異常系と制限 → 開発運用。

---

## 製品ビジョン・設計指針

> **AI時代の管理者（PL／テックリード）が、AIを含む少数精鋭チームを率いるためのローカルな計画表＋課題表。人間はGUIで、AIは素のJSONと `CLAUDE.md` で、同じ1枚を編集する。**

- **対象ユーザー**：巨大プロジェクトのエンタープライズPMではなく、**AIを含む10人規模の少数精鋭を率いる管理者（PL／テックリード）**。作者自身がこのペルソナ＝ドッグフーディング。
- **ビジョン（賭けと自覚）**：AI台頭で少数精鋭がAIを使いこなすのが主流になり、**全員がPL/TL化**する。そのリード（人間 **and** PL/TLエージェント）が計画と課題を管理する道具。未来への賭けだが、外れても今日のリードに刺さる＝ヘッジ済み。
- **核の差別化＝2つの第一級インターフェース**：人間＝GUI／AI＝素のJSON＋AI可読な `CLAUDE.md`。**両方が第一級**。全機能が GUI経路 と AI/JSON経路 の両方で動く（カスタムキー保持・round-trip）。
- **`CLAUDE.md` は"ドキュメント"でなく"AIのAPI仕様"**：AIがフォーマットに依存するため、**スキーマはほぼ動かさない**を厳守。変える時は読む側の後方互換を保ち、`CLAUDE.md`／`CLAUDE.en.md` を同時に更新する。
- **1つの入口で完結させる**：AI は別リポの説明書を読めない。**計画も課題も本ファイル1本**に書き、参照は文書内で完結させる。
- **正本ルール＝リポ単位で宣言・1リポ1正本**：課題の置き場を**リポジトリごとに1つだけ**決めて宣言する。GitHub Issue と課題表を混ぜない（どちらが本当か分からなくなる）。宣言は各リポの `CLAUDE.md` に書く（本リポの宣言は末尾の「このリポの正本」節）。
- **作る**：事実だけを持つデータ（状態は導出）／載せる規律を支える欄（二つの問い・完了条件）／止まっている理由が必ず添う「待ち」と「凍結」／AIネイティブ（進捗査定・依存推定）／担当別の負荷（人間 vs AI の内訳）／コスト（`_ai` トークン・`_money`）／誠実なview（見栄えでなく真実の状態）。
- **作らない**：エンタープライズなチケット管理／エンタープライズPM（ポートフォリオ・複雑権限）／ワークフロー・承認フロー／大規模リアルタイム協業／通知・外部送信／自動集計レポート。
- 意味論の出典 → [課題管理とはなにか？](https://knowledge.piguo.org/notes/what-is-issue-management/)
- 計画側の背景の論考 → [WBSという至高ツールで、このAI時代をサバイブする](https://zenn.dev/piguolabo/articles/99b5b30a028f80)

---

## 取り扱いの原則（重要）

- **ビューア（`wbs_viewer.html`）は原則さわらない。** 表示ロジックは完成済み。
- **変更は `wbs.json`（データ）の編集のみ。** AI に作業を頼むときも、原則 JSON を直す。
  - 表示仕様そのものを変えたい場合に限り HTML を触る（その時は明示的に依頼）。
- **派生値をデータに書かない。** 状態・期限超過・★（どの行に付くか）・工数・進捗率はビューアが計算する。`status` や `overdue` のようなキーを足さない。
  - **例外は3つだけ**（いずれも派生値ではなく、**設定**か**ある時点の事実**）：
    1. **`star`**（★を付ける報告期間）＝**設定**。報告する人と読む人・AIが同じ★を見るために JSON に持つ。
    2. **`_progress`／`_progressAt`／`_progressBy`**（計画の出来高査定）＝**ある時点の人/AI の判断という事実**。再計算しないので `_` キーで持つ（→ 「計画（`tasks`）のフィールド定義」）。
    3. **`_planLog`**（予定変更の履歴）＝**いつ・なぜ後ろ倒したかという事実**。append-only（→ 同上）。
- **課題（`issues`）と計画（`tasks`）で書き分ける**。ふだん課題の依頼で書き換えるのは `issues` と、その中の `links` だけ。**計画側を書き換えてよいのは次の3つ**：
  - **「組み込む」時**：対応する葉がまだ無ければ、**葉を1つだけ足してよい**。`id` は**案件の中で重複しない採番**、`name` は課題タイトル、`qty`／`hours`／`plan` は**利用者に聞く**（勝手に決めない）。案件に `tasks` が無ければ `tasks: []` を新設して足す。葉を足したら、**課題の `links` に `{ "wbs": "<足した id>" }` を必ず追加**する（足しっぱなしにしない）。→ 依頼例⑦-4
  - **「計画のタスクが終わった／着手した」の記録**：`actual.end`（と空なら `actual.start`）を書く。→ 依頼例⑯・「計画（`tasks`）の追加・更新 ①」
  - **計画そのものの依頼**（タスク追加・リスケ・進捗査定・マイルストーン・アーカイブ）：→ 「計画（`tasks`）の追加・更新」
- **つながりは課題側にだけ書く**。計画のタスクに「課題 #3」を書き足さない（画面が `links` から逆引きして札を出す）。
- **同じことを計画と課題の両方に書かない**（分担の詳細 →「課題管理の4つの動き ⇄ JSON 操作」の末尾）。

---

## AI が迷わないためのルール

**このファイルを読んで JSON を直す前に、まずこの表を読む。**

| 迷いどころ | ルール |
|---|---|
| 行動と WBS の子タスクの二重化 | **WBS の葉 ＝ 課題1件**（予定・実績・工数はここ）。**課題の行動 ＝ その課題の中の手順**（WBS に子は作らない） |
| 番号の種類 | WBS は文字列 `"2.3"`（鍵 `wbs`）、課題は整数 `3`（鍵 `issue`）。**鍵の名前で区別する** |
| 案件をまたぐ参照 | 同じ案件は `{ "issue": 3 }`、別案件は `{ "project": "…", "issue": 3 }` |
| `null` と「無い」 | 同じ意味。**AI が書く時は明示的に `null`** |
| 順番 | **配列順＝表示順・時系列**。末尾に足す。並べ替えない |
| 担当 | 自由文（`ぴぐお/Aさん`）。集計を頼まれたら割り切って文字列一致 |
| 方針は1つ？ | **1課題に方針を複数持ってよい**（`decisions[]`）。まだ決まっていないものは **`decided: null` の未決項目**として積む。**課題を分ける必要はない** |
| 状態の呼び名 | 状態は **未着手／対応中／完了** の3つだけ。**「未決」「待ち」「凍結」は状態ではなく印**（同じ課題に同時に付く） |
| 止まっている理由 | **人の返事や物を待っている＝待ち**（`kind: "waiting"`・誰を／何を／いつまで）。**意図的に止めている＝凍結**（`kind: "frozen"`・再開条件）。**決める人の判断待ちは待ちではなく `decisions` の未決** |

**編集したら検査する**：`uv run python scripts/check.py wbs.json`（uv が無い環境なら `python3 scripts/check.py wbs.json`。依存ゼロ・番号の重複／日付／enum／リンク先の存在／旧キーの残存を見る）。

---

## 依頼が曖昧・矛盾する時の作法（AI 向け）

**迷ったら編集せず聞く。** 以下は AI チェック（説明書だけを渡した AI に、利用者役の雑な依頼を出す検証）で実際に間違えた／判断の根拠が説明書に無かった場面。

1. **番号だけの依頼で、同じ番号が複数の案件にある** → **案件名を聞く**（直前の会話で案件が決まっている時は除く）。
   例：「#1 止まってる。開発部待ち」→「販売管理システム移行の #1 と 勤怠システム更改の #1 があります。どちらですか」
2. **言葉（タイトルの一部）で複数に当たる** → 候補を並べて聞く。1件に絞れる**明確な根拠**がある時だけ進め、**その根拠を返事に書く**。
   例：「文字コードのやつに1行足して」→ 2件当たるなら候補を出す。直前に #3 の話をしていたなら「直前の話が #3 なので #3 に足しました」と添える。
3. **番号なしの口語** → 候補を出したうえで、**「課題ごと閉じるのか、行動が1つ済んだのか」**を聞く。
   例：「勤怠のやつ終わった」→「勤怠システム更改の #2 でしょうか。課題を閉じますか、行動1件を済にしますか」
4. **依頼の番号と内容が食い違う** → **編集せず**、食い違いを示して聞く。**嘘の記録を書かない**。
   例：「#2 を解決で閉じて。リハーサルでエラー0件だった」→ #2 が帳票の課題なら「#2 は帳票の話です。リハーサルは #1 では？」
5. **済んだ行動（`done: true`）の日付・本文・担当は事実の記録** → **書き換えない**。直すのは利用者が「**訂正**」と明示した時だけ。
   例：「#1 の担当を全部佐藤に」→ **未の行動だけ**を佐藤にし、「済んだ2件は記録なので据え置きました」と返事に書く。
6. **同じ依頼の中で矛盾している** → 推測で後勝ちにせず聞く。
   例：「次の行動を担当 Aさんで追加。あと #1 の担当を全部佐藤に」→「いま足す行動の担当は Aさん のままでよいですか、佐藤に揃えますか」
7. **起票なのに完了条件や影響が無い** → **聞いてから載せる**。ビューアは `closeWhen` が空でも表示するが、**AI が載せる時は空にしない**（→「起票時のチェック」）。
   例：「帳票が文字化けした。10/20 に本番。載せといて」→「放置すると、いつ何が起きますか／どうなったら閉じられますか」を先に聞く。**似た課題が既にあれば「新規に載せますか、既存の #N に行動を足しますか」も併せて聞く**。
8. **新規課題をどの案件に入れるか分からない**（案件が2つ以上ある）→ 聞く。案件が1つしか無ければ聞かない。
9. **新しい行動は末尾に足す**。調整中（`date: null`）の行の**後ろ**に日付付きが来てよい。**並べ替えない**（配列順＝時系列の記録）。
10. **更新期間は `from`・`to` の両端を含む**。「次の週にして」は `from` を**いまの `to` の翌日**に、`to` を**`from` + 6日**にする（→ 依頼例⑩）。
11. **読むだけの依頼**（報告して／挙げて／何が動いた／壊れていないか）では **JSON を変えない**。返事に **「変更なし」** と1行書く。
12. **やることは分かるが、必要な番号が無い**（例：「組み込んで」で WBS 番号が無い）→ **既定の動きを示して確認する**。既存の葉があれば「`2.3` に紐づけます」、無ければ「課題タイトルで葉を1つ足します」と案を出す。**予定日・工数など分からない値は聞く**（仮の数字を入れない）。
13. **連名の担当**（`ぴぐお/Aさん`）を「全部○○に」と言われたら、**文字列ごと置き換えるのが既定**（`佐藤`）。「Aさんだけ外して」のように**1人だけ差し替える意図に見える時は聞く**。
14. **「閉じて」と明示されたが、中身が終わっていない**（未了の行動が残る／未決が残る）→ **閉じる前に食い違いを示して聞く**。番号がはっきりしていても、黙って閉じない。
    例：「#1 を解決で閉じて」→「#1 は未の行動が2件、未決が2件残っています。**それでも閉じますか**（完了条件は満たしていますか）／**先に未決を決めますか**」
15. **待ちの経過報告**（「#3 まだ返事来ない」）→ **勝手に何かを変えず、3択で聞く**。①**期限内ならデータは変更なし**（「9/12 までなのであと3日あります。このままにしますか」）②**期限を延ばす**（`pending.until` を新しい日に）③**催促した事実を行動に足す**（`actions` に「催促」の1行）。
    「まだ来ない」は**状況の共有であって指示ではない**ことが多い。**変更なしで返す時は「変更なし」と書く**。

16. **`what`／`q` に理由を捏造しない**。利用者が言っていない因果（「〜のため」「〜が原因で」）を足さず、**言われたことの言い換えに留める**。
    例：「#3 まだ開発部から返事が来ない」→ `what` は「開発部からの回答」。**「開発部が多忙で」は書かない**（聞いていない）。背景が要るなら聞くか、`note` に「利用者談」と分かる形で書く。
17. **「来週」の起算**は次で固定する（`decisions[].until`／`pending.until`／`due` に共通）。
    **「来週」＝発話日の翌週の月〜日**／**「来週金曜」＝翌週の金曜**／**「今週」＝発話日を含む週（月〜日）**。
    「来週中に」のように**曜日が決まらない言い方は、その週の金曜か日曜かを聞く**（勝手に決めない）。日付が確定したら `YYYY-MM-DD` で書く。
18. **査定を頼まれたが、成果物が示されていない／葉が未着手**（`actual.start` が空）→ **`_progress` を書かない**。「まだ着手前です」と答え、**何を見て査定するか**（実装・テスト・doc・コミット等）を聞く。
    **0% を書いて「査定済み」にしない**（`_progress: 0` は「見たうえで0%」という事実の記録であって、「まだ見ていない」とは違う）。未着手なら進捗は自動で0%になるので、書く必要もない。
19. **AI が `_planLog` や `_progress` を書くときの `by`／`_progressBy` は、`"manual"` ではなくモデル名**（例 `"claude-sonnet-5"`）。`"manual"` は**人が手で直した時だけ**。誰の判断かが残らないと、あとで査定を見直せない。
20. **計画をまたぐ読み取り依頼**（「今月やること」「遅れているもの」「進捗を査定して」など）で、**案件に `tasks` が無ければ「計画（WBS）が無いため判定できません」と一言添える**（黙って省かない）。
    該当0件のときも **「遅れているタスクはありません」と明記**する。読むだけの依頼なので **JSON は変えない**（→ 11）。
21. **既に日付が入っている `until`／`due` を、まとめて動かす依頼**（「未決の期限を全部来週にして」「期限を一律1週間延ばして」）→ **上書きする前に、対象を列挙して確認する**。
    期限を延ばすのは**約束の変更**であって、記録の整理ではない。とくに**いま超過しているもの**は「延ばす」と催促の印が消えるので、**何件が超過中か**を数えて並べ、そのうえで「全部延ばしますか／超過中は据え置いて残りだけ延ばしますか」と2択で聞く（おすすめは後者）。
    例：「未決の期限を全部来週金曜に」→「対象は5件（うち2件は 9/4・9/8 で超過中）です。**超過中の2件は据え置き**、残り3件を 9/18 にするのがおすすめですが、5件とも動かしますか」
22. **計画の予定（`plan`）を動かす依頼は、理由の有無で2つに分かれる**。
    **理由が添えられていれば リスケ**＝`plan` を新しい日付に更新し、**`_planLog` に1行追記**する（いつ・どこから どこへ・±N日・理由・`by`）。
    **理由が無ければ 訂正**＝`plan` を直すだけで**履歴は残さない**（入力ミスの直しに履歴を積むと、後から「なぜ延びたか」が読めなくなる）。
    **どちらか迷えば聞く**（「後ろ倒しの理由を1行いただければリスケとして履歴に残します。入力ミスの訂正なら履歴なしで直します」）。`_planLog` の形と `by` の値は 19 と「計画（`tasks`）のフィールド定義」が正。
**判断に迷ったら編集せず聞く。聞き返しは2〜3択にして、おすすめを1つ添える。**

---

## 動作環境
- **Google Chrome（最新版）推奨**。Edge 等の **Chromium 系でも動作**（検証は Chrome で実施）。Firefox/Safari は不可（File System Access API 未対応）。
- 会社管理ブラウザはポリシーで File System Access が無効だと編集不可（閲覧は可）。
- HTTPサーバー不要・外部ライブラリ/CDN不要。**`wbs_viewer.html` を file:// でそのまま開く**。
- **GitHub Pages（https）に置いても同じ HTML がそのまま動く**（[デモ](https://piguo45.github.io/single-file-wbs/demo.html)）。外部送信のコードを持たないので、開いたデータはブラウザの中だけで処理される。

## 開き方・更新
1. Chrome で **`wbs_viewer.html`** を開く。
   **「計画｜課題」の2択スイッチ**が操作バーにある（計画側の「時間／進捗」タブと同じ作法・片方が塗り）。
   **スイッチが出るのは「課題を出しうる時」だけ**：`issues` がある JSON（計画と両方でも課題だけでも）と、**まだ何も読み込んでいない時**（両方押せる）。**`tasks` しか無い JSON ではスイッチを出さない**（計画だけを出す）。**`issues` しか無い JSON ではスイッチは出るが「計画」側が無効**（押せない）。両方あれば既定は**前回見ていた側**（localStorage 記憶）。
2. **「ファイルを開く」** で `wbs.json` を選ぶ（同じボタンへの**ドラッグ&ドロップ**でも読み込める）。
3. `wbs.json` を編集して保存 → **「更新」ボタン**で再読込・再描画（再選択不要 / File System Access API）。
4. スクロール位置は**折りたたみ・更新では保持**、**新規ファイル読込時のみ本日へセンタリング＋折りたたみリセット**（計画側）。

画面の読み方（列・タブ・折りたたみ・絞り込み・リンク）は次の「表示」節にまとめてある。

---

## 表示

### 共通
- **UIは日本語/英語切替**（操作バーの「EN / 日本語」ボタン）。既定は日本語・選択は localStorage に記憶。
  文言は HTML 内の `I18N` テーブルに集約（データ＝`wbs.json` の中身は翻訳しない）。**UI文言を追加する時は ja/en 両方に追加**。
- **簡素表示トグル**：ヘッダ左の**タイトル（ロゴ/「WBS Viewer」）をクリック**すると、ロゴ・バージョン・"Viewer"・タブ名を隠して **「WBS」だけ**にする（再クリックで戻す・localStorage記憶）。製品感を消し、制約の強い現場で"自作ツール"として通すための逃げ道。発見性を上げないため**ツールチップは付けない**。
- ブラウザのタブ名は「`name` – WBS Viewer」。
- 絞り込みは**軸内OR・軸間AND・既定＝絞らない**を計画側と課題側で共通の作法にする（全列Excel風は作らない＝軽量・誠実を保つ）。
- **フィルタは表示専用＝`wbs.json`（データ）は不変**。行を隠すだけで、時間軸も件数サマリも動かない（隠す＝目隠しであって削除でない）。

### 計画（`tasks`）の画面
- **状態フィルタ**：**左の情報表の真上の「フィルタバー」**（操作バー下の全幅帯）に置く「状態：未着手／進行中／完了」の3トグル（ピル形）で**状態ごとに表示/非表示**（既定=全ON・localStorage記憶 `wbsStateFilter`・i18n ja/en）。完了が積み上がっても未完にフォーカスできる。**表示専用＝データも横軸（時間範囲）も不変**：横軸は全データ基準で固定し、残ったバーは1pxも動かない。状態は既存の進捗ロジックの排他3分割（未着手=`actual.start`なし／進行中=startあり・endなし／完了=`actual.end`あり）を `STATES` テーブルで判定。**親残し判定**＝配下に表示対象が1つでも残れば親は残す／全部対象外なら親（・案件）ごと隠す。**右上サマリ（工数/進捗/遅延）は完了込みのまま不変**（誠実なview）。非表示中のピルは**取り消し線**で色以外でも判別（CUD冗長化）。
- **遅延フィルタ**：同じフィルタバーの「遅延のみ」トグル（既定OFF・localStorage記憶 `wbsDelayOnly`）。ONで**遅れているリーフだけ**表示＝遅れ＝`slip>0`（進捗遅れ）または**期限超過**（本日>予定終了かつ未完）。状態と別軸なので**軸間AND**で重なる（例：進行中 かつ 遅延）。ON時のピルは**赤系**（意味色：遅延=赤）で、単独トグルなので取り消し線は出さない。
- **担当フィルタ**：担当は可変・多数なのでピルでなく**ドロップダウン（`details/summary`・ブラウザ標準）＋チェックリスト**。担当値は `wbs.json` から**動的収集**（ユニーク・空担当＝「(未割当)」・ソート）。**複数選択（軸内OR）**＝チェックした担当だけ表示・**全選択/全解除**あり・既定=全員表示。バッジは全員／件数`(N)`／1人なら名前。**off集合方式**（非表示にした担当を localStorage `wbsAsgOff` に記憶＝新担当は既定で表示されて堅牢）。ドロップダウンはチェックしても**開いたまま保持**。
- **期間フィルタ**：**今日／今週／今月／全期間**の**単一選択セグメント**（既定=全期間・localStorage記憶 `wbsPeriod`）。ONで**予定 or 実績の期間が対象範囲に重なるリーフだけ**表示（今週=本日を含む月〜日／今月=本日を含む**暦月**＝1日〜末日・会計月や任意開始日は対象外）。**横軸は動かさず行だけ絞る**。選択中は塗り表示で単一選択を明示。
- このフィルタバーで**PMの絞り込み4軸（状態→遅延→担当→期間）が完成**する。
- 折りたたみ：**◆案件・1L・2L の `▼/▶`〜名前をクリック**。**作業項目ヘッダの `▼/▶`** で全展開/全たたみ（▼=全部開いている→押すと全たたみ／▶=どこか閉じている→押すと全展開）。ヘッダ操作の誤りは **Ctrl+Z で直前の表示状態に復元**（入力フォーカス中は無効）。
- 列見出しは中央寄せ。左の情報表（No.〜備考）は固定、**横スクロールはガントだけ**。
- **予定/実績の日付列は2段見出し**：上段に「予定」「実績」、その下に「開始／終了」（`COLS` の `group` プロパティで隣接列をまとめる）。
- **列の折りたたみ（アウトライン式）**：列ヘッダ上の薄い1段に **+/−** を置き、**折りたたみ単位**ごとに畳む/展開。単位は**8グループ**＝`数量+時間`（工数の内訳）／工数／進捗／状況／担当／予定／実績／備考（**No.・作業項目は常時表示**）。畳んだ列は **0幅で消す（隙間スタブを作らない）**、+ は境界に絶対配置。状態は localStorage 記憶・スクロール/ツリー折りたたみは保持・畳むとガント領域が広がる。`COL_CG` マップ＋`effCols()` フィルタで実装（描画は列数ぶん＝行数依存の負荷なし）。
- **列幅の変更**：列ヘッダの**境界をドラッグ**して幅を変える（ホバーで青い細線＝掴める合図）。**ダブルクリックで既定幅に戻す**（Excel慣習）。幅は localStorage 記憶（`wbsColWidths`・key→px。既定＝`COLS` の `w`／下限＝`COLS` の `min`）。**列の折りたたみとは独立**（畳んだ列の幅も保持され、開くと戻る）。編集モードの必要幅（日付84px・数量/時間52px の下限、作業項目の +72px）は従来どおり優先。幅の実体は CSS 変数 `--cw-<列key>` で、ドラッグ中は**変数の書き換えだけ**（再描画なし）。確定は mouseup で1回だけ再描画。**上限＝左ペインの合計が `画面幅 − 200px` まで**（ガントを最低200px残す）。起動時にも同じ判定をして、**画面に収まらない保存値は既定に戻す**。
- 行のレイヤ色：**◆案件（上下に区切り線）＞ 1L ＞ 2L ＞ 3L**。
- ガント：日付＋曜日（年月は年あり）、**土日＋祝日（`holidays`）の列を全高で薄ピンク**（`--weekend`／半透明オーバーレイで行の上に重畳・バー/イナズマ/本日線は前面で視認性維持）、進行中は実績を本日まで。日付ヘッダの**祝日は赤字**（土＝青字・日＝赤字の慣習は維持）。祝日の名称は日付セルのツールチップに出す。
  **予定=塗りなしの枠（前面・4辺）／実績=塗りバー（背面）を同一レーンに重ねる**：実績が枠の右を超える＝**終了遅延＝赤バー＋「+N」**（数字のみ・一律「線の先」に表示・正確値はツールチップ「終了遅延 +N日」）、枠の左が空く＝**着手遅れ**（実績開始が枠の左より右）として直接見える（着手遅れは専用色を持たず空きで表現）。遅延を色分けした（赤=終了遅延・グレー=完了）ので、枠＋塗りでもプログレスバーには誤認されない。
- **親（集約）＝細いサマリーバー**：予定枠も実績も細く（形で葉と区別・色は増やさない＝CUD安全・折りたたみ時も全情報を保持）。実績の充填比率は葉と同じ。**バー色は全階層で同一**（親子はバーの太さ＝形で区別、色相・輝度では区別しない）。
- **配色（CUD配慮）**：実績=青／予定=青の枠／終了遅延・イナズマ=赤（進行中=鮮赤／完了済み=くすみ赤）／完了=グレーのバー。**進捗タブだけは実績/不足分に専用色**（`--actual-soft`/`--short-soft`＝時間タブと同じ色相・明度で彩度だけ50%低い）を使う＝意味色は共通のまま面積の圧を下げる。着手遅れは色を使わず「枠の空き」で表現。Okabe-Ito原則に沿い「青と紫」を避け（MS既定色はモーブ `#cc79a7`）、色だけに頼らず形・位置・ラベルで冗長化。検証は `tests/e2e/test_color_audit.py`（課題側は `tests/e2e_issue/test_color_audit_issue.py`。色を足す/変える時は必ずペアを追加して再実行）。
- 日付列（予定/実績）は `5/11` 表示。初期表示は本日が中央付近。
- **完了タスク**：行を濃いめグレー＋取り消し線＋先頭 `✓`。ガントの実績バーも**グレー**（青=進行中／グレー=完了 を一目で）。
- **備考内の URL は自動リンク**（`http(s)://` のみ・新規タブで開く）。
- オーバーレイ（SVG）：
  - **イナズマ線**（赤・**全案件通しで1本**）。各 terminal 行の点を次で決める（**左へ突出＝遅延**）：
    - 完了 ＝ 本日線上
    - **終了遅延**（着手済み・未完了で本日 > `plan.end`）＝ **予定終了日**（締切超過を優先表示）
    - **着手遅延**（未着手で本日 > `plan.start`）＝ **予定開始日**
    - 進行中（期限内）＝ 本日線上（着手ズレは表示しない）
    - それ以外（未着手で未到来 等）＝ 本日線上
    - 折りたたんだノードは集計値で1点。本日線は引かない（基準＝本日）。
  - **マイルストーン線**（各案件の `milestones`・任意色。**既定色＝Okabe-Ito モーブ `#cc79a7`**）。
- **右ペインは「時間／進捗」タブ切替**（既定=時間）。時間＝ガント（無改変）／進捗＝完了率の横バー（青=実績／赤=予定への不足分・赤の右端＝あるべきPV／子=10px・親=6pxの細いサマリー＝**時間ビュー（14px/8px）より細い**＝色の面積を減らす）。一度に描くのは片方だけ（非アクティブ側はDOM注入しない＝描画負荷は1ビューぶん）。タブ選択は localStorage 記憶。
- **進捗ビューの見分け（時間ビューと差別化）**：①**直角バー**（時間ビュー＝角丸／形で区別・CUD安全）②**青グレーの未達トラック**（0-100%の下地 `#eef1f7`）③**定規状の目盛り**＝主目盛り（全高・点線・20/40/60/80% `#555`）＋細目盛り（上端から6割の短い実線・10%刻み・同色 `#555`・バー前面に重畳）。軸ラベルも 0/20/40/60/80/100%。④**専用色＋細いバー**＝実績 `--actual-soft` `#6872b1`／不足分 `--short-soft` `#b15a5d`。**色相と明度(L*)は時間タブの青 `#2f6fed`・赤 `#e11d48` と同値のまま、彩度(C*)だけ50%低い**。進捗タブは全行が0-100%のフル幅バーで色の面積が時間タブの数倍あり原色だと画面が重いため。圧の元は**彩度と面積の両方**なので、バー高も 14/8px→**10/6px** に細くして併せて減らす（下地`.ptrack`・細目盛りも追随）。**明度は下げない**（薄くすると遅れの印象まで弱くなる）＝意味色は不変で圧だけ下げる。

### 課題（`issues`）の画面
- 案件（タブ）：`projects` が2件以上あると表の上に**タブバー**が出る（先頭の**サマリ**＋案件ごとのタブ。タブ名の右に `★N ⚠N`）。
  **サマリ**は案件ごとの集計表（案件名／**未着手**／**対応中**／**完了**／`待ち`／`凍結`／`未決`／**★ 更新**／**⚠ 期限超過**／次の期限＋合計行）。行をクリックするとその案件へ移る。
  **描くのは開いている案件だけ**なので、案件が増えても重くならない。
- 列は **No／優先度／タイトル／詳細／状態／期限** の6列（左＝**何の話か**／中＝**どう進んでいるか**／右＝**今どこか・いつまで**）。
  **詳細列は本文だけ**：概要（**支障**／**価値**／完了条件／方針／待ち・凍結／完了）→ 実績 → 予定。タイトルはタイトル列に出るので詳細では繰り返さない。
  **備考（`note`）は普段は出さない**（課題の中身ではなく添え書き）。**印はタイトル下のリンク行の末尾**に置く（詳細の末尾ではない）。**マウスを置くと全文のツールチップ**、**クリックすると吹き出しが固定**されて中の URL を押せる（外側クリック／`Esc` で閉じる）。リンク行に出るので**たたんだままでも読める**。
  区切りは **`概要` `実績` `予定` の帯（ラベル）＝灰の塗り・枠なし**。`■` のような記号も、区切り線も使わない。
  **詳細の中は「役割ごとに表現を1種類」**：帯＝塗り／★＝**黒の記号**／`済`・`未`＝**文字の濃淡**（済＝濃い灰の太字・未＝薄い灰。英語UIは ☑／☐）。備考の印は**帯より薄い塗り**。
  **★は全箇所で黒**（詳細の行動行・タイトルの★・タブの★・右上の `★ 更新 N` すべて。色は付けない）。
  **枠線は優先度・状態のバッジ専用**。詳細の中では塗り・枠・取り消し線・大きさの差を使わない。
  行動の担当は **`担当：ぴぐお`** の形で書く（空ならラベルごと出さない）。
- 折りたたみ：**タイトルのセルをクリック**でたたむ／開く（折りたたみの `▼/▶` と未回答の `⚠` も**タイトル列**に出る）。
  **たたむと詳細列が「次の一手」1行だけ**になる（例：`未 9/11 資料作成、内部Rv 担当：ぴぐお/Aさん`）。未の最初の行動で、無ければ**空欄**（完了の課題は常に空欄）。
  **タイトル列の見出しの `▼/▶`** で全展開／全たたみ。
- 絞り込み：表の上の**フィルタバー**（状態／優先度／担当／**印**）。
  **印は6つのピル**：`待ち`／`凍結`／`未決`／`期限超過`／`催促`／`★ 更新`。**「のみ」は付けない**（`期限超過のみ` ではなく `期限超過`）。押した印が付いている課題だけを出す、という意味はピルの ON/OFF で分かるため。
担当は**行動に出てくる全員**から集める（軸内OR。行動担当のどれかがONなら、その課題を表示）。担当列は無いが、絞り込みは行動の担当で効く。**表示専用で `wbs.json` は変わらない**。並び替えは `No（データ順）／期限／優先度`。
  状態／優先度／印のフィルタと並び順は**ブック共通**、担当の候補と折りたたみの開閉は**案件ごと**。右上の件数は**開いている案件**のもの。
- リンクをたどる（**課題 → 計画**）：タイトルの下のリンク行（**1行1リンク**で `WBS 2.3`／`課題 #1`／`勤怠-2`／`仕様書` が縦に並ぶ）をクリックすると飛ぶ。
  **すべて同じページの中の移動**（別タブは開かない）。`WBS 2.3` を押すと**「計画」に切り替わり**、案件を合わせ → その行を開き → スクロール → 2秒ほど強調。`課題 #1` は「課題」の中で同じことをする。
  URL の末尾に **`#wbs=案件名/2.3`** または **`#issue=案件名/番号`** が書き込まれ、ブラウザの**「戻る」で元の行に戻れる**。この URL を人に渡すと、相手はファイルを開いた時に同じ行へ飛ぶ（未読込なら「○○ を表示するには JSON を開いてください」の案内）。
  外部URL（`{ title, url }`）だけは新しいタブで開く。リンク行は**1行1リンク**で、**末尾に `備考` の印**が並ぶ。
- 逆引き札（**計画 → 課題**）：課題からリンクされている計画タスクの行に、小さな **`課題 #3`** の札が出る（複数なら `課題 #3 #7`）。
  **計画側のデータには何も書かない**＝描画のたびに `links` から逆引きして作る。タスク名のクリックは折りたたみ操作なので、**札のクリック**で課題へ飛ぶ。
  リンク先が無い（そのタスク・課題・案件が存在しない）→ **薄い取り消し線＋ツールチップ**（クラッシュしない・データは消さない）。

---

## ブラウザ上で編集（編集モード）

テキスト/AI 編集に加えて、**画面上から直接 `wbs.json` を編集**できる（任意機能）。
**「編集」ボタン**でON/OFF（ONで緑）。変更は少し置いて `wbs.json` へ自動保存（保存状態はヘッダ右に常時表示）。

### 計画（`tasks`）でできること
- **各フィールドのインライン編集**（No.=id／作業項目名／数量／時間／担当／予定・実績の日付／備考）。
- **進捗の入力**：リーフの `◀ N% ▶` ステッパーで±10%＝`_progress`。10％以上にすると着手日 `actual.start` に本日を自動セット（0％に戻しても着手日は残す）。
- **リーフ追加**：行の `＋`＝下に兄弟、案件行の `＋タスク`。id は既存と重複しないよう採番（折りたたみ中の追加は自動展開）。
- **入れ子（子タスク）追加**：行の `＋子`。リーフに押すと**集計ノード化**して元の値を子1へ引き継ぐ＝工数・進捗は不変。既存の集計には末尾に空の子を追加。子id＝親id＋連番。**3階層目には出さない**＝4階層を作らせない。
- **削除**：`✕`（確認あり・配下ごと）。**集計ノードの最後の子を消すと、その子の値が親へ戻り親はリーフに降格**＝工数が消えない。
- **兄弟内の上下移動**：`▲▼`。
- **マイルストーン編集**：案件行の `＋MS`／直下の編集行で **日付(📅)・名前・色・✕削除**。色は **Okabe-Ito 由来のCUD安全5色から選択**＝GUIは自由hexでなく選択式・JSON/AIは任意 `#hex` 可。
- **リスケ**：リーフ行の `↷`＝予定変更を履歴 `_planLog` に記録して予定を自動更新（フォーム＝新開始・新終了・理由(任意)→「リスケ確定」で `_planLog` 追記＋`plan` 自動更新＋自動保存が1クリック）。**日付セルの直接編集は「訂正」＝履歴に残さない**（リスケと意図を分離）。詳細は「計画（`tasks`）のフィールド定義」の `_planLog`。
- **範囲外（＝やらないこと）**：ドラッグ&ドロップ／親をまたぐ移動／自動リナンバー。これらは JSON か AI 編集で。
- 工数・進捗・イナズマ線は従来どおり自動再計算（編集中は入力フォーカスが外れてから再描画）。

### 課題（`issues`）でできること

| 対象 | 操作 |
|---|---|
| タイトル | **タイトル列**でその場で書き換え |
| 備考 | 編集モードでは**詳細列の末尾に textarea が出る**（読むときの印・吹き出しの代わり。改行は保持）。**課題に担当欄は無い**＝担当は詳細の行動ごとに編集する |
| 優先度 | 高／中／低 のプルダウン |
| 期限（`due`） | `YYYY-MM-DD` のテキスト＋📅。`611`・`6/11` の短縮入力も可 |
| 二つの問い・完了条件 | `ifIgnored`／`ifDone`／`closeWhen` を複数行で編集（改行は保持） |
| 行動（`actions`） | `＋` 追加／`済⇄未` チェック／日付・本文・担当の編集／`✕` 削除／`▲▼` 並べ替え／`WBS` タグ編集 |
| 決めること（`decisions`） | **`＋方針`** で1件追加。編集行は**1行レイアウト**で、左から **`確定`ボタン ／ `q`（決めるべきこと） ／ 期限欄 ／ `✕`**。期限欄の placeholder は **`期限 MM-DD`**（空なら `until` は `null`）。行頭の **`確定`** を押すと**答えのフォーム**が開き、そこに `a`（方針）を書くと `decided` に今日が入る。**「決める」ボタンは廃止**（行頭は `確定`）。**`✕`** でその行を削除 |
| 待ち / 凍結 / 解除 | **`待ち`** → 誰を（`who`）・何を（`what`）・いつまで（`until`）を入力すると `pending = { kind: "waiting", … }`。**`凍結`** → 再開条件（`resumeWhen`）と理由（`detail`）で `pending = { kind: "frozen", … }`。解除は `pending = null` |
| クローズ / 再オープン | ボタン（画面名 **`クローズ`**）→ 閉じ方3択・確かめた事実 を入力すると `closed` をセット（**同時に `pending` を `null` に戻す**＝依頼例③と同じ）／再オープンは `closed = null` のみ |
| 更新期間（`star`） | ヘッダのメタ行の `更新期間 9/1〜9/7` から `from`／`to` を編集（`to` を空にすると本日まで） |
| リンク（`links`） | **`＋リンク`** ボタン → 4形のどれかを選んで入力（`wbs`／`issue`／別案件／外部URL）。各リンクの **`✕`** で削除。リンク行は**1行1リンク** |
| 課題の追加・削除 | **`＋課題` は絞り込みバーの右端**（編集モード中だけ出る）。`id`＝既存最大+1・`opened`＝今日・`priority`＝中。**追加後は自動でその行へ移動**する／削除は `✕`（確認あり・**タイトル列**） |

### 案件（`projects[]`）でできること

| 対象 | 操作 |
|---|---|
| 案件を追加 | `＋案件` → 名前を入力。**旧形式のファイル（`{ project, tasks }` など）は押した時点で `projects` 形式に変換**される |
| 案件名の変更 | タブを**ダブルクリック**。**別案件からのリンクは名前で指している**ので、変えたら指している側も直す（`scripts/check.py` で確認できる） |
| 案件の削除 | タブの `✕`（確認あり。**中の課題と計画ごと**消える） |

**できないこと**（JSON直編集か AI に依頼する）

- ドラッグ&ドロップでの並べ替え／`id` の自動振り直し／複数ファイルを同時に開く／通知・外部送信。

**保存と権限（共通）**

- **自動保存**：変更は約0.4秒のデバウンス後に `wbs.json` へ書き戻し（File System Access API）。書込は単一キューで直列化。**除外するのは内部派生値のみ**（`_calc`/`_leaf` 等）＝**`_` 始まりのユーザーキーは保持**される。保存状態（未保存の変更あり… ／ 保存済 HH:MM:SS ／ 保存失敗）は**ヘッダ右上に常時表示**。
- **外部変更の検知**：保存直前にファイルの更新時刻を確認し、ツール外（AI編集等）で変わっていたら**上書き確認**を出す。AI に編集を頼む時は編集モードをOFFにしておくのが安全。
  - **上書き確認でキャンセルした後に「更新」を押すと、画面で編集した内容は捨てられる**（ファイル側が正になる）。「更新」を押す前に**確認が出る**ので、画面側を残したいならキャンセルして、先に別名で保存するか AI 側の変更を取り込む。
- **横入りの自動リトライ**：同期（OneDrive 等）やウイルス対策がファイルに触れた一瞬と保存が重なると、ブラウザが `InvalidStateError`（「state had changed since it was read from disk」）で書込を弾くことがある。この時は**1拍おいて1回だけ自動で再保存**を試みる（再試行の `getFile()` でブラウザの内部スナップショットが更新され、ほぼ回復）。書込前に弾かれるため**原本は無傷**。再試行も失敗したら「保存失敗」を表示し**未保存フラグを保持**（次の編集／タブ離脱で再保存。アラート連打は避けるため自動の連続再試行はしない）。
- **権限**：保存には**書き込み権限**が要る。「ファイルを開く」or D&D で開いた**ハンドルがあるファイルのみ**編集可。**`file://` では書き込み許可のプロンプトが出せない**ため、**編集ON時に保存ダイアログで同じ `wbs.json` を選び直す**（選択自体が許可になる）。**https（GitHub Pages 等）では書き込み許可のプロンプトが出せる**ので、保存ダイアログでの選び直しは要らない（`requestPermission` で済む＝段階的フォールバックの本来の経路）。⚠ Chrome は**ピッカーで選んだ時点でファイルを切り詰める**ため、選択直後に現在のデータを**即保存**する（空のまま残さない）。「もう一度押してください」と出たら**再度「編集」を押す**（ブラウザのジェスチャー制限。2回目は直接ダイアログが開く）。この許可は**セッション限り**（Chrome を再起動したら、編集ONのたびに再選択が必要）。**新規ファイルを読み込むと編集モードは自動OFF**（再度ONで新ファイルの権限を取得）。読み込み失敗時はハンドルを差し替えない（別ファイルを誤上書きしない）。
- **旧フォーマット**（計画だけの単一プロジェクト形式 `{ project, tasks }`）は編集ON時に確認の上、**`projects[]` 形式へ変換**してから編集する。
- **日付欄は `YYYY-MM-DD` のテキスト入力＋📅ボタンでカレンダー**。`2026/07/01` 形式も受け付けて `-` に正規化（`input type=date` の表示形式はブラウザUI言語依存で制御不能なため、表示をISO固定にする設計）。
- 入力ガード：日付は **1900〜2099年のみ有効**（範囲外・入力途中の値は無視して保存しない）。数量/時間は小数可（`step="any"`）。number 入力上のホイールは無効（誤変更防止）。タブを閉じる際、未保存があれば確認が出る。
- 状態遷移は**ボタン1つで必要な事実をまとめて書く**（片手落ちを構造的に防ぐ）。手で `closed` だけ書くと `decisions` の未決が残ったままになることがある。

---

## データ（wbs.json）

### 形

```json
{
  "name": "自分の課題管理表",
  "star": { "from": "2026-09-01", "to": "2026-09-07" },
  "projects": [
    {
      "name": "販売管理システム移行",
      "milestones": [
        { "date": "2026-10-20", "label": "本番切替", "color": "#cc79a7" }
      ],
      "tasks": [
        {
          "id": "2",
          "name": "変換ツール",
          "children": [
            { "id": "2.3", "name": "文字コード変換", "qty": 1, "hours": 16,
              "assignee": "ぴぐお",
              "plan": { "start": "2026-09-14", "end": "2026-09-18" },
              "actual": { "start": null, "end": null }, "note": "" }
          ]
        }
      ],
      "issues": [
        {
          "id": 1,
          "title": "移行テストで文字コード起因のエラーが大量発生",
          "priority": "high",
          "opened": "2026-09-05",
          "due": "2026-10-20",
          "ifIgnored": "本番の切替で同じエラーが起き、移行が失敗する。影響が出るのは本番切替日",
          "ifDone": "",
          "closeWhen": "リハーサルで一連の移行手順を通して、エラーが出ないこと",
          "decisions": [
            { "q": "対応方針", "since": "2026-09-05",
              "decided": "2026-09-07", "a": "原因調査と変換処理の修正で対応する" },
            { "q": "変換ツールを内製するか、既存の変換ライブラリを使うか", "since": "2026-09-07",
              "decided": null, "a": "" }
          ],
          "pending": null,
          "closed": null,
          "links": [
            { "wbs": "2.3" },
            { "issue": 3 },
            { "project": "勤怠システム更改", "issue": 2 },
            { "title": "仕様書", "url": "https://example.com/spec" }
          ],
          "actions": [
            { "date": "2026-09-05", "text": "事象発生", "assignee": "", "done": true },
            { "date": "2026-09-07", "text": "調査方針検討、影響調査", "assignee": "ぴぐお", "done": true },
            { "date": null, "text": "文字コード仕様について開発部と打合せ", "assignee": "", "done": false }
          ],
          "note": "関連リンクは links に持つ（備考は添え書き）",
          "_ai": { "tokens": 1200, "model": "claude-fable-5-1", "memo": "起票をAIが下書き" }
        }
      ]
    }
  ]
}
```

**案件（project）＝ `projects[]` の1要素**。計画（`tasks`）と課題（`issues`）が**同じ案件の中に並ぶ**。
`tasks` だけの案件（計画だけ）・`issues` だけの案件（課題だけ）・両方ある案件、どれも有効。
`star`（更新期間）と `holidays`（祝日）は**ブック共通**（トップレベル）。`milestones` は案件ごと。

### フィールド定義

| キー | 型 | 必須 | 意味（＝日常語） |
|---|---|---|---|
| `name` | string | 任意 | ブック名（画面とブラウザのタブに出る） |
| `holidays` | array | 任意 | 祝日（ブック共通・トップレベル）。形は `["YYYY-MM-DD", { date, name }]`。計画の描画で使う（→「計画（`tasks`）のフィールド定義」） |
| `star` | `{ from, to }`／`null` | 任意 | **更新期間**（＝報告期間）＝★を付ける範囲（`name` と同じトップレベル）。`from`＝開始日、`to`＝終了日（**省略＝本日**）。無ければ既定＝**直近7日**（本日を含む）。**派生値ではなく設定**（下の「報告する」節） |
| `projects[]` | array | 必須 | **案件**の配列。1案件＝ `{ name, milestones?, tasks?, issues? }`。タブの並び順＝配列順 |
| `projects[].name` | string | 必須 | 案件名（タブに出る）。**別案件からのリンクはこの名前で指す**ので、**ブックの中で一意**にする |
| `projects[].milestones` | array | 任意 | その案件のマイルストーン（`{ date, label, color }`）。計画側で使う |
| `projects[].tasks` | array | 任意 | **計画（WBS）**。`id` は文字列 `"2.3"`・親子ネスト（最大3階層）・`plan`／`actual` を持つ。形は →「計画（`tasks`）のフィールド定義」、計算式は →「計算ロジック」。課題の依頼では原則**読むだけ**（リンクの照合と札の表示）。書き換えてよい3つの場合は →「取り扱いの原則」 |
| `projects[].issues` | array | 任意 | その案件の**課題**の配列。**配列順＝表示順** |
| `id` | number（整数） | 必須 | No。**案件の中で**一意（案件が違えば同じ番号でよい）。欠番可。**振り直さない** |
| `title` | string | 必須 | 見出し（全角30字目安。詳細は他欄へ） |
| `priority` | `"high"`／`"mid"`／`"low"` | 任意（既定 `mid`） | 優先度＝影響の大きさ×期限の近さ。画面は 高／中／低 |
| `opened` | `YYYY-MM-DD` | 任意 | 載せた日 |
| `due` | `YYYY-MM-DD`／`null` | 任意 | **期限＝支障や価値が現れる日**（`ifIgnored` の支障が出る日、または `ifDone` の価値を受け取りたい日。担当者がいつやるかではない） |
| `ifIgnored` | string | 二者択一 | **放置すると、いつ・何が起きるか**（妨げる側の問い）。例：「10/20 の本番切替で帳票が読めなくなる」。**画面の見出しは `支障：`**（鍵の名前は変えない） |
| `ifDone` | string | 二者択一 | **やると、いつ・何が得られるか**（加速させる側の問い）。例：「11月から、毎月の締め作業が半日で終わる」。**画面の見出しは `価値：`**（鍵の名前は変えない） |
| `decisions[]` | array | 任意 | **決めること／決めたこと**の配列。1課題に**複数持てる**。要素は `{ q, since, until, decided, a }`：`q`＝**決めるべきこと**（問い）／`since`＝**いつから決まっていないか**／`until`＝**いつまでに決めたいか**（任意・`null` 可。過ぎると **⚠ 催促**）／`decided`＝**決めた日**（`null` なら**未決**）／`a`＝**方針**（決めたら書く）。**配列順＝表示順**、末尾に足す |
| `closeWhen` | string | 必須（空でも可・空は警告表示） | **完了条件**＝弊害が起きない／価値が実現したと確かめられた状態。※「空でも可」は**ビューアの許容**（壊れた入力で落ちないため）。**AI が起票する時は空にしない**（→「起票時のチェック」「依頼が曖昧・矛盾する時の作法」7） |
| `pending` | `{ kind, … }`／`null` | 任意 | 進められない理由。**2形だけ**。**待ち**＝`{ kind: "waiting", since, who, until, what }`（`who`＝待っている相手・`what`＝何を・`until`＝いつまで〈`null` 可〉）／**凍結**＝`{ kind: "frozen", since, resumeWhen, detail }` |
| `pending.who` | string ／ `{ issue }` ／ `{ project, issue }` | 待ちで必須 | **人なら文字列**（`"開発部"`）、**課題を待っているなら課題参照**（`{ "issue": 2 }`／別案件は `{ "project": "…", "issue": 2 }`）。課題参照にすると、**相手が完了した時に ⚠ 催促**が出る（待ち続ける理由が消えたのに解除していない、という合図） |
| `closed` | `{ at, how, note }`／`null` | 任意 | **閉じる**。`at`＝閉じた日、`how`＝下表の3値、`note`＝**確かめた事実**（`closeWhen` と対応させて書く） |
| `links[]` | array | 任意 | **リンク台帳**。この課題がつながる先を並べる。要素は次の4形のどれか（**鍵の名前で種類を区別し、混ぜて書けない**）：`{ "wbs": "2.3" }`＝同じ案件の計画タスク／`{ "issue": 1 }`＝同じ案件の課題／`{ "project": "勤怠システム更改", "issue": 2 }`（`"wbs"` 版も可）＝別案件／`{ "title": "仕様書", "url": "https://…" }`＝外部URL（**`title` 必須・`http(s)` のみ**）。**つながりは課題側にだけ書く**（計画側には書かない） |
| `actions[]` | array | 任意 | 実績と予定（Excel の 実績／予定 の段）。**配列順＝時系列の表示順**。画面では `実績`（済）と `予定`（未）の帯で分けて出す |
| `actions[].date` | `YYYY-MM-DD`／`null` | 任意 | 日付。**`null`＝調整中（未定）** |
| `actions[].text` | string | 必須 | 何をした／する |
| `actions[].assignee` | string | 任意 | その行動の担当（`"ぴぐお/Aさん"` のような複数表記は自由文字列）。**担当はここだけに持つ**（課題レベルの担当欄も担当列も無い）。詳細の行動行に **`担当：ぴぐお`** の形で出る（空ならラベルごと出さない）。たたむと詳細列が次の一手1行になり、そこにも同じ形で出る |
| `actions[].done` | boolean | 任意（既定 `false`） | 済／未。画面は **`済`＝濃い灰の太字／`未`＝薄い灰**の**文字の濃淡だけ**で分ける（英語UIは **☑／☐**）。塗り・枠・取り消し線・大きさの差は使わない |
| `note` | string | 任意 | 備考（添え書き）。**画面では常時表示せず**、詳細の末尾の `備考` の印から**ツールチップ（ホバー）／吹き出し（クリックで固定）**で読む。`http(s)://` は自動リンクになり、**吹き出しの中で押せる** |
| `_任意名` | any | 任意 | カスタムキー（ビューア無視・保存で保持） |

**`pending.kind`（止まっている理由・2種）**

| 値 | 日常語 | 形 | 打ち手 |
|---|---|---|---|
| `"waiting"` | **待ち**（相手の返事・物・時期を待っている） | `{ kind, since, who, until, what }` | 相手に催促する（`until` を過ぎると **⚠ 催促**） |
| `"frozen"` | **凍結**（意図的に止めている） | `{ kind, since, resumeWhen, detail }` | 再開条件を書いて寝かせる |

**相手も中身も書かれていない待ちは、誰も催促できないまま止まり続ける**ので、`who` と `what` は空にしない。凍結の `resumeWhen` も同じ。
記事の**ペンディング3理由**は、本ツールでは次のように分かれる（**打ち手は記事のまま**）。

| 記事の理由 | 本ツールでの書き方 | 打ち手 |
|---|---|---|
| 決まっていない（未決） | **`decisions[]` の未決項目**（`decided: null`）。ペンディングではない | 決められる人に持ち込む |
| 揃っていない（人・モノ・情報・時期） | **待ち** `{ kind: "waiting", since, who, until, what }` | 相手に催促する（`until` を過ぎると **⚠ 催促**） |
| 止めている（意図的な凍結） | **凍結** `{ kind: "frozen", since, resumeWhen, detail }` | 再開条件を書いて寝かせる |

**`closed.how`（閉じ方）**

| 値 | 日常語 | いつ使うか |
|---|---|---|
| `"resolved"` | 解決 | 完了条件（`closeWhen`）が満たされたと確かめた |
| `"wontfix"` | 対応しない | 「やらない」と決めた（決めた時点で閉じる） |
| `"not_occurred"` | 発生せず | 期限が過ぎて、書いた支障が起きなかった（**行は消さない**） |
| `"duplicate"` | 重複 | 同じ話が既に別の課題にある。**元の課題への `links`（`{ issue }` か `{ project, issue }`）が必須**。以後はそちらで追う |

### 計画（`tasks`）のフィールド定義

- 各案件の `tasks` は親子ネスト（**最大3階層**）。`children` あり＝**集計ノード**、なし＝**リーフ**（工数を持つ）。
- リーフのフィールド：
  ```
  id, name, qty(数量), hours(1単位あたり時間), assignee(担当),
  plan:   { start, end }        予定 "YYYY-MM-DD"
  actual: { start, end }        実績（未定は null）
  note
  _任意名                       "_"始まりはカスタムキー（任意・ビューア無視・保存で保持）
  ```
  - **`id` は文字列**（`"2.3"`）。課題の番号は整数（`3`）で、鍵の名前（`wbs` / `issue`）で区別する。**案件の中で一意**にする。
  - **`qty`（数量）は繰り返し単位**（例：画面5枚 × `hours` 4h＝工数 20h）。**単発の作業なら `1` のまま**でよい。使わないなら画面の「数量+時間」列を畳める（列の折りたたみ・#64）。
  - **`name` は簡潔なラベルにする（目安：全角14字以内。詳細・経緯は `note` へ）** — 長くても壊れないが、一覧の作業項目列で見切れる（全文はホバー表示）。
- **工数・進捗率はデータに持たせない**（すべて派生値。ビューアが算出 → 「計算ロジック」）。
- **祝日（`holidays`・ブック共通・トップレベル）**：`holidays: ["YYYY-MM-DD", { date, name }]`（文字列形＝名称なし／オブジェクト形＝`name` でツールチップ）。
  全案件共通。日付ヘッダで**祝日＝赤字**、ガントで**土日＋祝日の列を全高で薄ピンク**に塗る。未指定なら土日のみ薄ピンク（後方互換）。
  例：`"holidays": [{ "date": "2026-07-20", "name": "海の日" }]`
- **`milestones: [{ date, label, color }]`** は各案件の中に任意（計画側の縦線として描く）。
- **例外：`_progress`（0/10/…/100）はビューアが読む**進捗の値（EV）。併せて `_progressAt`（査定日時 ISO）・`_progressBy`（**誰が査定したか**）を持つ。
  **`_progressBy` は、AI が書くときはモデル名**（例 `"claude-sonnet-5"`）。**`"manual"` は人が手で（画面のステッパー等で）直した時だけ**＝AI 編集で `"manual"` と書かない。
  AIへの依頼例：「タスクXの進捗を成果物と要件から最寄り10%で査定して」→ リーフに `_progress`/`_progressAt`/`_progressBy` を書く（→「計画（`tasks`）の追加・更新 ⑥」）。未設定なら進捗は時間ベースにフォールバック（後方互換）。
  査定は**事実の記録**（その時点の人/AI 判断）。再計算しないので `_` キーで持つ＝「派生値を持たせない」方針と矛盾しない。
- **`_planLog`（予定変更の履歴）**：遅延で後続を後ろ倒した経緯を**事実として残す** `_` キー（`_progress`/`_ai` と同思想・派生でない・ビューアは既定で保持）。
  リスケのたびに1エントリ**追記（append-only）**。**先頭エントリの `from` が実質ベースライン（当初計画）**：
  ```json
  "_planLog": [
    { "at": "2026-06-10T09:00:00Z",
      "from": { "start": "2026-06-01", "end": "2026-06-05" },
      "to":   { "start": "2026-06-15", "end": "2026-06-19" },
      "by": "manual", "reason": "#504 の遅延で後ろ倒し" }
  ]
  ```
  - `at`＝変更日時ISO／`from`・`to`＝変更前後の `plan`／`by`＝**誰が直したか**／`reason`＝理由（遅延波及元の `#N` 等）。
    **`by` は、AI が書くときはモデル名**（例 `"claude-sonnet-5"`）。**`"manual"` は人が手で（編集モードの `↷` 等で）直した時だけ**＝AI 編集で `"manual"` と書かない。
  - **AIネイティブ**：「B を1週間後ろ倒して、理由＝#504遅延 と記録して」→ AI が `plan` を更新し `_planLog` に1行追記。
  - **GUI経路（編集モード）**：リーフ行の **↷ ボタン → フォーム（新開始・新終了・理由(任意)）→「リスケ確定」**＝`_planLog` 追記＋**`plan` 自動更新**＋自動保存が1クリック（片手落ちを構造的に防ぐ）。**日付セルの直接編集は「訂正」＝履歴に残さない**（リスケと意図を分離）。変更なしは記録しない。履歴のGUI削除は無し（直すならJSON）。
  - **表示**：直近の修正1本だけ**トレイル**（点線グレー・下レーン=開始の変化・上レーン=終了の変化＝出る場所で種別が読める）。タスク名横に **↷N**（有効エントリ数）。**↷N かリスケ済み予定バーをクリック**で吹き出し＝全履歴（いつ・元→新・±N日・理由）＋当初比。外側クリック/Escで閉じる。行は増やさない・修正が何回でもガントのインクは一定。トレイル起点は時間軸の範囲に自動で含める。壊れた要素は無視（graceful・`tests/異常_リスケ履歴.json`）。
  - スコープ：記録は**予定(`plan`)の変更に限定**（全フィールド監査ログは作らない）。

### `_` キー（カスタムキー）

- **`_` 始まりのキーは自由に追加してよい**。ビューアは原則無視し、ブラウザ編集の保存でも保持される（round-trip）。
- メタ情報の置き場に使う（例：`_ai` ＝ AI作業のトークン記録、`_money` ＝ コスト）。構造も自由。課題にも計画のリーフにも付けられる。
- **原則として派生値の置き場ではない**。状態・期限超過・★（どの行に付くか）・工数・進捗率は `_` キーであっても書かない（★の**期間**はトップレベルの `star` に持つ）。
- **ビューアが読む `_` キーは3つだけ**（いずれも派生値ではなく、**ある時点の事実**なので持ってよい）：
  - **`_progress`**（0/10/…/100）＝出来高の査定値。併せて **`_progressAt`**（査定日時ISO）・**`_progressBy`**（`"manual"` or モデル名）。
  - **`_planLog`**＝予定変更の履歴（append-only）。
  - どちらも「計画（`tasks`）のフィールド定義」に形がある。
- **`_calc`／`_leaf` は内部予約名**。ビューアが描画のたびに作る作業用の値で、**どの階層に書いても保存で除かれる**（案件・タスク・課題のどこでも）。**利用者キーに使わない**（書いても消える）。

### 後方互換の約束

- **既存キーの意味は変えない。** 追加はしても、削除・改名・型変更はしない。
- **新しいキーは任意（省略可）にする。** 古い `wbs.json` がそのまま読めることを保証する。
- スキーマを変える時は `CLAUDE.md` と `CLAUDE.en.md` を**同じコミットで**更新する。
- 独自の情報は**まず `_` キーで持つ**。正式なキーに昇格させるのは、他のリポでも要ると分かってから。

**古いファイルの読み方（読む側の責任）**

| 入力 | 扱い |
|---|---|
| v2.0.0 の形 `{ name?, holidays?, star?, projects: [{ name, milestones?, tasks?, issues? }] }` | そのまま |
| 計画だけのファイル `{ projects: [{ name, milestones, tasks }] }`（single-file-wbs の既存データ） | そのまま開ける。課題は空表示 |
| 計画側の旧形式 `{ project, milestones, tasks }`（単一プロジェクト） | 読み込み時に `projects[]` 形へ変換して開く（編集ON時は確認を出す） |
| `_progress`／`_progressAt`／`_progressBy`／`_planLog` を持つ既存ファイル | **そのまま読む**（ビューアが読む3つの `_` キー。→「`_` キー（カスタムキー）」） |
| 知らない `_` キーを持つファイル | **無視して保持**（保存で消さない＝round-trip） |

**課題側に旧形式は無い。** 課題は v2.0.0 が最初の公開形なので、`sheets`／1枚形の `issues`／`actions[].wbs`／課題レベルの `assignee`／`decision`／`decisionLog`／`pending.reason` は**読み替えない**（リリース前に消したので、混ざったファイルは存在しない）。
**AI は新形式だけを書く。** これらのキーが残っていたら検査スクリプトが**エラー**にする。

---

## 計算ロジック（計画側・ビューアが決定論的に算出）
- **工数（人日）= `qty × hours ÷ 8`**。親 = 配下リーフの合計。
- 親の予定/実績期間 = 配下リーフの最小start〜最大end。
- 進捗率（EV＝実績の出来高・基準日＝本日）：
  - 未着手（actual.start なし）= 0% ／ 完了（actual.end あり）= 100%
  - 進行中 = **`_progress`（0/10/…/100）があれば最寄り10%に量子化して採用**。無ければ時間ベース
    `clamp((本日 − actual.start) ÷ (plan.end − plan.start) × 100, 0, 100)`（後方互換のフォールバック）。
  - 親 = 配下リーフの (progress × 工数) の加重平均（連続値・刻みに丸めない）。
- **EVM（出来高管理）の3値**：EV＝実績(進捗)／**PV＝予定**＝本日あるべき% `clamp((本日−plan.start)/(plan.end−plan.start)×100,0,100)`（直線・親は加重平均）／
  **slip＝遅れ＝max(PV−EV,0)**（EVMのSV相当）。S字曲線・非線形PV・コスト軸は非対象（やらない）。
- **ヘッダ右の全体サマリ（計画側）**：**期間**（最早開始-最遅終了／残り営業日＝土日**＋祝日(`holidays`)を除外**）／**工数**（人月＝人日÷20稼働日／残り）／
  **進捗**（実績EV%／予定PV%／遅れ=max(PV−EV,0)%）の3行＋下段メタ（📄ファイル名・N案件・📅本日・🔄更新・💾保存）。各行ホバーで補足。
- **遅延バッジ（全体サマリの4行目「遅延：」）**：全体の遅れ%は教科書EVM（前倒し分で相殺＝0%もあり得る）なので、見落とし防止に
  **遅れているタスク数**をバッジで出す（**期限**＝本日>予定終了かつ未完了の件数／**進捗**＝PV>EVの件数）。件数0の種別は非表示・両方0なら行ごと非表示。
- **進捗列＝`◀ N% ▶` ステッパー**（10%刻み・編集モードのリーフのみ操作可／親は自動集計の読み取り＝連続値）。`_progress` は派生でなく
  「ある時点の人/AIの判断」という事実なので `_` キーで持つ（再計算しない）＝「派生値を持たせない」方針と矛盾しない。
- **状況列＝遅れ/実績/予定**（slip/EV/PV の%）。遅れ=赤・実績=青・予定=黒。**遅れてない行は実績のみ**（静か）。暗算不要（遅れを計算済みで提示）。
- **予定終了の赤**：本日 > 予定終了 かつ 未完了＝締切超過（完了行は赤にしない）。期日の遅れは時間タブのガント/イナズマと共通の「赤＝遅れ」。

---

## 計画（`tasks`）の追加・更新（やり方）

編集は `wbs.json` のみ。保存 → 画面の **「更新」** ボタンで反映。
課題（`issues`）側の依頼は →「AI への依頼例（課題側・原則 JSON を編集させる）」。

### ① 状態の更新（日々の運用・最重要）
進捗率・工数・イナズマ線は**自動計算**。手で触るのは **「実績の日付」だけ**：
- **着手したら** → そのリーフの `actual.start` に日付（→ 実績終了列が「進行中」・イナズマ線に反映）
- **完了したら** → `actual.end` に日付（→ 行がグレー＋`✓`・イナズマ点は本日線）
- **予定変更** → `plan.start` / `plan.end` を直す

### ② 作業項目（タスク）を追加
集計ノードの `children` にリーフを1件足す。
**`name` は簡潔なラベルにする（目安：全角14字以内。詳細・経緯は `note` へ）** —
長くても壊れないが、一覧の作業項目列で見切れる（全文はホバー表示）。
```json
{ "id": "2.1.3", "name": "新規作業", "qty": 1, "hours": 16, "assignee": "ぴぐお",
  "plan":   { "start": "2026-07-01", "end": "2026-07-05" },
  "actual": { "start": null, "end": null }, "note": "" }
```
- 中間の工程を増やすなら、`children` を持つ**集計ノード**を足してその下にリーフを入れる（最大3階層）。
- **課題に紐づく葉を足すとき**は、足しっぱなしにせず課題の `links` に `{ "wbs": "<足した id>" }` を書く（→「AI への依頼例（課題側・原則 JSON を編集させる）⑦-4」）。**WBS の葉＝課題1件**なので、課題の中の手順を子タスクにしない（手順は課題の `actions[]`）。

### ③ 案件を追加
`projects` 配列に1件足す（課題側の作法は →「AI への依頼例（課題側・原則 JSON を編集させる）⑪」。案件名は**ブックの中で一意**にする）：
```json
{ "name": "新案件", "milestones": [],
  "tasks": [ { "id": "1", "name": "フェーズ1", "children": [ /* リーフ… */ ] } ] }
```

### ④ マイルストーンを追加
該当案件の `milestones` に：
```json
{ "date": "2026-09-30", "label": "リリース", "color": "#ef4444" }
```

### ⑤ AI への依頼例（計画側）
- 「設計レビューを**今日完了**にして」→ 該当リーフの `actual.end` に本日を入れる
- 「コンポーネント配置に**着手**」→ `actual.start` を入れる
- 「テストフェーズを**追加**して」→ 集計ノード＋リーフを追記
- 「5月以前の完了を**アーカイブ**して」→ →「アーカイブ運用」（バックアップ＋削除）
- 「タスクXの進捗を**査定して**」→ 下記⑥「AI 進捗査定ワークフロー」（成果物＋要件→最寄り10%で `_progress`）
- 「B を1週間**後ろ倒し**て、理由＝#504遅延」→ `plan` を更新し `_planLog` に1行追記（→「計画（`tasks`）のフィールド定義」）

### ⑥ AI 進捗査定ワークフロー（このツールの核＝AIネイティブ）
人手の「だいたい何%」をAIに任せる。手順は決定論的に：
1. **対象リーフの要件を読む**（紐づく課題の `closeWhen`・`note` のURL・受け入れ条件、関連コミット/成果物）。逆引きは `links[].wbs` がこの葉を指す課題。
2. **成果物と要件を突き合わせ**、達成度を**最寄り10%（0/10/…/100）に量子化**して見積もる（過大評価しない・未検証は積まない）。
3. リーフに3キーを書く：`_progress`（査定値）／`_progressAt`（査定日時ISO）／`_progressBy`（査定したモデル名。人手なら `"manual"`）。
4. **完了は別**：実際に終わったら `_progress` ではなく `actual.end`（=100%確定）。`_progress` は「進行中の出来高見積り」専用。
- 依頼例：「`2.3` の進捗を成果物（実装/テスト/doc）と完了条件から最寄り10%で査定して」
- 注意：査定は**事実の記録**（その時点の人/AI判断）。再計算しないので `_` キーで持つ＝「派生値を持たせない」方針と矛盾しない。

---

## 導出値（課題側・ビューアが計算・JSONに書かない）

| 導出値 | 規則 |
|---|---|
| **状態（3つ・排他）** | `closed` あり → **完了**（`how` で 解決／不対応／発生せず のサブ表示）／ else **済んだ行動**（`done: true`）が1件でもある → **対応中**／ else → **未着手**。**待ち・凍結・未決は状態ではない**（下の「印」） |
| **印（6つ・状態と直交）** | ①**待ち**（`pending.kind === "waiting"`・`who` を添える）②**凍結**（`pending.kind === "frozen"`）③**未決 N**（`decisions` の `decided: null` の件数）④**⚠ 期限超過**（本日 > `due`）⑤**⚠ 催促**（次の3つのどれか＝**待ちで 本日 > `pending.until`**／**未決で 本日 > `decisions[].until`**／**待ちの相手が課題で、その課題が完了している**。下の「⚠ 催促」の行が正）⑥**★ 更新**（報告期間に動いた）。**完了の行には ①〜⑤ を出さない**（もう追いかけない）。**★だけは完了にも付く**（「今週これを閉じた」が報告の中身だから） |
| **⚠ 催促** | 次のどれかで `⚠ 催促` の印。①**待ち**（`kind: "waiting"`）で **本日 > `pending.until`**／②**未決**（`decided: null`）で **本日 > `decisions[].until`**／③**待ちの相手が課題**（`who` が課題参照）で、**その課題が完了している**（待つ理由が消えたのに解除していない）。`until` が `null` なら①②は出さない（期限を切っていない待ちは催促しようがない）。**件数と絞り込みは待ちと未決の両方を数える** |
| **経過日数** | **未着手**の行に `opened` からの日数、**待ち**の行に `pending.since` からの日数、**未決**の各行に `decisions[].since` からの日数を小さく添える。表記は置き場所で2通り。**状態列の印は `未着手 N日`／`待ち N日`**（起算日は出さず、ツールチップで補う）。**`N日経過（M/D〜）` の形で出るのは「方針（未決）」の帯の各行だけ**（例：`14日経過（8/26〜）`。`M/D` は起算日）。どちらも**表示モードと編集モードで同じ**。**放置の長さが数字で出る**ので、古い順に手を付けられる |
| **方針の行**（概要の中） | `decisions` のうち **`decided` が入っている項目**を「**方針：a（M/D）**」として**1件1行**で並べる（複数可・配列順）。**表示モードでは `a`（または `q`）が空なら行ごと出さない**が、**編集モードでは出す**（壊れた項目を画面から直せなくしないため） |
| **「方針（未決）」の帯** | `decisions` のうち **`decided: null` の項目**を、`概要` の下の帯にまとめて **「・」で列挙**。各行に **`since` からの経過日数**を `N日経過（M/D〜）` の形で添える（例：`・変換ツールを内製するか 14日経過（8/26〜）`）。1件も無ければ帯ごと出さない |
| **期限超過** | 本日 > `due` かつ 未完了 → `due` を赤字＋`⚠` の印（完了行は赤にしない・印も出さない） |
| **★ 更新**（報告期間に動いたもの） | 期間内に**済んだ行動**（`done: true` かつ `date` が期間内。`date: null` には付かない）／期間内に**決めた**（`decisions[].decided`）・**止めた**（`pending.since`）・**閉じた**（`closed.at`）に ★。★が1つでもある課題は**タイトルに ★**（たたんでも見える）。期間は `star`（無ければ直近7日）で、**`from` と `to` の両端を含む**（`from` 当日・`to` 当日も対象）。画面の呼び名は「**更新**」（タブ・タイトル・詳細行では ★ の記号だけ出し、ツールチップに「更新…」を出す） |
| **次の一手** | `done: false` の最初の1件（サマリ・ツールチップに使う） |
| **たたんだ時の詳細列** | **次の一手**（`done:false` の最初の1件）を1行で表示：`未 9/11 資料作成、内部Rv 担当：ぴぐお/Aさん`。未の行動が無ければ**空欄**（完了の課題は常に空欄）。**JSONに書かない**（課題は担当欄を持たない・独立した担当列も無い） |
| **リンク行** | `links[]` を**タイトルの下**に、小さく薄い文字で並べる。**1行に1リンク**（`WBS 2.3`／`課題 #1`／`勤怠-2`／`仕様書` がそれぞれ1行）。**別案件の課題は「案件名の先頭2文字＋番号」**（勤怠システム更改の #2 → `勤怠-2`）。**データは不変**で、表示だけを縮める（AI は正式に「勤怠システム更改 #2」と書く）。**末尾に `備考` の印**（`note` があるとき）。序列は**タイトル ＞ リンク**（濃さと大きさで弱める。**枠・札は使わない**＝枠はバッジ専用）。**たたんだ時も出す**。ホバーで下線＋少し濃く。リンク先が無い（そのタスク・課題・案件が存在しない）→ **薄い取り消し線＋ツールチップで理由**（クラッシュしない・**データは消さない**） |
| **WBS 側の逆引き札**（統合後） | 課題からリンクされている計画タスクの行に、小さな `課題 #3`（複数なら `課題 #3 #7`）の札を出す。**計画側のデータには何も書かない**＝描画のたびに `links` から逆引きして作る。タスク名のクリックは折りたたみ操作なので、**札のクリック**で課題へ飛ぶ |
| **未回答の警告** | `ifIgnored` と `ifDone` が両方空／`closeWhen` が空 → 行に薄い警告マーク（**エラーにはしない**） |
| **件数サマリ** | ヘッダ右は**2段**。上段＝**状態**（`未着手 N`／`対応中 N`／`完了 N`）、下段＝**印**（`待ち N`／`凍結 N`／`未決 N`／`⚠ 期限超過 N`／`⚠ 催促 N`／`★ 更新 N`）。**下段の印はすべて「課題の件数」**＝`未決 N` は**未決の方針を持つ課題の件数**（1課題に未決が3件あっても 1 と数える）。**項目の件数を出すのは行のバッジの `未決 N`** だけ（サマリ表の列・タブのバッジも課題の件数）。**0 の印は並べない**のは `待ち`／`凍結`／`未決`／`⚠ 期限超過`／`⚠ 催促` の5つ（件数 0 ならヘッダから消える）。**`★ 更新` だけは 0 でも常に出る**（「この期間は動いていない」も報告の中身だから）。**完了込みで不変なのは上段の状態3つ**（印①〜⑤は完了の行に出ないので、閉じれば下段は減る）。メタ行に **`更新期間 9/1〜9/7`**（既定のときは `更新期間 直近7日`） |

**状態は3つで排他**（上から順に1つだけ当たる）。**「どこまで手が動いたか」だけ**を表す。

**待ち・凍結・未決は状態ではなく印**（横断的な事実）。だから **対応中でも未決が3件ある**、**未着手でも待っている**、が同時に成り立つ。
実際そうなっている表を「未決」や「保留」という1つの箱に押し込めると、動いているのか止まっているのかが読めなくなる。

**「実行が終わったら閉じる」型（TODO的な行）に、決める作業は要らない**：
「載せた時点で `decision` を書く」規則は**設けない**。`decisions` が空なら**未決0件**として扱い、
状態は済んだ行動の有無だけで **未着手／対応中** に決まる（迷う余地の無い行を無理に「決めた」ことにしない）。

---

## 課題管理の4つの動き ⇄ JSON 操作

管理とは4つの動きの繰り返し：**載せる → 決める → 組み込む → 閉じる**。

| 動き | やること | 書くキー |
|---|---|---|
| **載せる** | 二つの問いのどちらかに答え、期限と完了条件を決めて表に載せる。**その場で決めるべきことがあれば `decisions` に未決で積む**（決めてから載せる必要はない） | `issues[]` に1件追加。`id`／`title`／`ifIgnored` **または** `ifDone`／`due`／`closeWhen`／`priority`／`opened`（担当は `actions[].assignee` に書く） |
| **決める** | やるのか、やらないのか。**決めるべきことが複数あってよい** | `decisions[]` に足す。まだ決まっていない＝`{ q, since: 今日, decided: null, a: "" }`（未決として積む）。決まった＝その項目に `decided`（決めた日）と `a`（方針）を入れる。「対応しない」と決めたら**同時に** `closed = { at, how: "wontfix", note }` |
| **組み込む** | 計画（WBS）に載せる。**WBS の葉＝この課題1件**（予定・実績・工数はそちら）。課題の中の手順は `actions[]` に書く | 課題の `links[]` に `{ "wbs": "2.3" }` を足す。**`actions[].wbs` は使わない**（廃止） |
| **閉じる** | 完了条件が満たされたことを**確かめて**閉じる | `closed = { at, how, note }`。`note` には `closeWhen` に対応する**確かめた事実**を書く |

**言葉の対応**：「載せる・決める・組み込む・閉じる」は**記事（課題管理論）の言葉**。**画面のボタンは `方針を決める`／`クローズ`**（en `Decide`／`Close`）。同じ動きを指す。

**止まったとき（ペンディング）** — 4つの動きのどこでも起こりうる横断的な状態：

| 状況（記事の3理由） | 本ツールでの書き方 | 併せて書く |
|---|---|---|
| 決められる人の判断待ち | **`decisions[]` の未決項目**（`decided: null`）。ペンディングにしない | `q` に「（誰の判断待ち）」を添える |
| 情報・人・モノ・時期が揃わない | **待ち** `pending = { kind: "waiting", … }` | `who`＝誰を／`what`＝何を／`until`＝いつまで（**`until` を過ぎると ⚠ 催促**） |
| 意図的に止めている | **凍結** `pending = { kind: "frozen", … }` | `resumeWhen`＝**再開条件（必須級）**／`detail`＝止めている理由 |

解除するときは `pending = null` に戻す（履歴は残さない）。**待ちと凍結は同時に持てない**（`pending` は1件）。

**課題表と WBS の分担**：課題表が持つのは「**何を、なぜ**」（`title`／二つの問い／`closeWhen`）。
WBS が持つのは「**いつ、誰が、どうやって**」（工数・日付・進捗）。**同じことを両方に書かない**。
繋ぐ鍵は課題の **`links[]`**（`{ "wbs": "2.3" }`）。**計画側には何も書かない**（画面が逆引きして札を出す）。

**閉じるときの注意**：アクションアイテムが全部終わっても課題は自動では閉じない。
`closeWhen` を確かめてから閉じる。期限が過ぎて何も起きなかった行は `how: "not_occurred"`（発生せず）で閉じる。**削除しない**（「起きなかった」という事実が残る）。

**表に載せないもの**：リスク（**起きたら**課題に載せる）と改善（**取り組むと決めたら**課題に載せる）。

---

## 報告する（週次定例と ★更新）

週次の定例で「**前回から何が動いたか**」を伝えるための印が ★。画面ではこれを「**更新**」と呼ぶ（右上の `★ 更新 3`・サマリの列見出し `★ 更新`）。
課題の進み具合ではなく、**期間の印**。期間は「**更新期間**」（`star`）。

> 「更新」を選んだのは、**済んだ・決めた・止めた・閉じた の4種すべて**をこの1語で呼べるから。`⚠ 期限超過` と並べたときに対になる。

- **更新期間を決める**：トップレベルの `star`（例 `{ "from": "2026-09-01", "to": "2026-09-07" }`）。`to` を省くと**本日まで**。`star` が無ければ**直近7日**。
  - **両端を含む**：`from` の当日と `to` の当日も期間の中（例の場合、9/1 と 9/7 に動いたものは ★ が付く）。
- **★が付くもの**（ビューアが判定・JSONに書かない）：
  - 期間内に**済んだ行動**（`done: true` かつ `date` が期間内。日付が `null` の行には付かない）
  - 期間内に**決めた**（`decisions[].decided`）・**止めた**（`pending.since`）・**閉じた**（`closed.at`）
  - ★が1つでもある課題は**タイトルに ★** が付く（たたんだ一覧のままでも読める）
- **定例の進め方**：
  1. 定例の前に **★の付いた課題を上から読む**（右上の `★ 更新 3` で件数、メタ行の `更新期間 9/1〜9/7` で期間を確認）。
  2. 報告する（AI に「今回の★を箇条書きで報告して」と頼める → 依頼例⑨）。
  3. 定例が終わったら **`star.from` を次の期間に進める**（依頼例⑩）。翌週は「前回から動いたもの」だけが ★ になる。
- ★が無い＝**その期間は動いていない**という意味。悪いとは限らない（凍結中の行など）が、★が付かない週が続く課題は、期限と `pending` を見直す合図。

---

## 起票時のチェック（載せる規律）

課題を1件足す前に、次の3つを確認する。**1つでも答えられなければ、まだ載せない。**

1. **二つの問いのどちらかに答えられるか**
   - `ifIgnored`（画面では `支障：`）：**放置すると、いつ・何が起きるか**
     例：「10/20 の本番切替で帳票が読めなくなる」
   - `ifDone`（画面では `価値：`）：**やると、いつ・何が得られるか**
     例：「11月から、毎月の締め作業が半日で終わる」
   - 現場の課題表の **「影響／効果」欄にあたる**。この製品では**放置側を「支障」、実行側を「価値」**と呼ぶ（どちら向きの欄かを名前で分かるようにするため）。
   - どちらにも答えられないものは課題ではない（メモや思いつき）。
2. **期限は「支障や価値が現れる日」か**
   - `due` は**担当者がいつやるか**ではない。問いの答えに出てきた「いつ」をそのまま入れる。
   - 例：「本番切替で失敗する」→ `due` は本番切替日。
3. **完了条件を書いたか**
   - `closeWhen` は「作業が終わった」ではなく「**確かめられた状態**」で書く。
   - 悪い例：「変換ツールを作る」／良い例：「リハーサルで一連の手順を通して、エラーが出ないこと」。

4. **同じものが既に載っていないか**
   - 同じ案件に、**同じ期限・同じ言葉**（例：「文字コード」）の課題が既にあるなら、**新規に載せるか、既存の課題に行動を足すか**を利用者に聞く。
   - **判定は「完了条件が同じか」**。言葉が似ていても**確かめたい状態が違えば別件**なので、そのまま載せて `links` で関連として結ぶ。
   - 重複起票は、同じ話が2か所で進んで両方が中途半端になる原因になる。
   - **載せた後に気づいたら**、新しい方を **`closed.how: "duplicate"`** で閉じ、`links` で元の課題を指す（→ 依頼例⑥-2）。

優先度は **影響の大きさ × その日の近さ** で決める（人／AI の判断値として `priority` に持つ。計算式は持たない）。

---

## アーカイブ運用（古い完了の退避）

項目が増えすぎないよう、**古い完了（計画のタスク・閉じた課題）は current から退避**する：

- **トリガー**：ガントの表示が進み、**過去の月（例：2026年5月以前）の完了タスク**が
  「過去日付以前」になったら実施。課題側は、閉じた行が一覧の大半を占めてきたら実施。
- **手順**：
  1. **同じフォルダ内にバックアップを作成**（例：`wbs-archive-2026-05.json` に退避分をまとめる。`wbs-archive-*.json` は gitignore 済み）。
  2. `wbs.json` から該当の**実施済み（完了）タスク**と、**閉じて久しい課題**（`closed` あり）を削除する。
- → current の `wbs.json` は「今と未来」に集中。履歴はアーカイブ JSON に保全。
- 大量項目対策は **折りたたみ（表示）＋このアーカイブ運用（データ）** の二段で行う。
- **閉じた課題を消す前に、他から指されていないか確かめる**（別課題の `links` / `pending.who`）。指されたまま消すと**壊れたリンク**になる（`scripts/check.py` で検出できる）。

---

## まず jq で拾う（ファイル全体を読まない）

課題と計画が同じ1ファイルに溜まると、正本の JSON は数百KBになる。
**読むだけの依頼は、まず `jq` で必要な部分だけ取り出す。ファイルを開くのは書き込む時だけ**（→「依頼が曖昧・矛盾する時の作法」11）。
例は本リポの正本 `wbs_roadmap.json`。**該当が無ければ何も出ない**ので、空なら「0件」と答える。

**① 課題の一覧**（案件名・番号・タイトル・閉じたかどうか）

```bash
jq -r '.projects[] | .name as $p | .issues[]? | "\($p)\t#\(.id)\t\(.title)\t\(if .closed then "完了" else "未" end)"' wbs_roadmap.json
```

**② 番号で1件だけ**（案件名で絞ってから番号を指す。番号は案件の中で一意）

```bash
jq '.projects[] | select(.name=="single-file-wbs 開発") | .issues[] | select(.id==37)' wbs_roadmap.json
```

**③ 未決の一覧**（`decided` が `null` の問いと、いつから決まっていないか）

```bash
jq -r '.projects[] | .name as $p | .issues[]? | select(.closed==null) | .id as $i
       | (.decisions//[])[] | select(.decided==null) | "\($p)\t#\($i)\t\(.q)\t\(.since)"' wbs_roadmap.json
```

**④ 待ちの一覧**（誰を・何を・いつまで）

```bash
jq -r '.projects[] | .name as $p | .issues[]? | select(.pending.kind=="waiting")
       | "\($p)\t#\(.id)\t\(.pending.who|tostring)\t\(.pending.what)\t\(.pending.until//"期限なし")"' wbs_roadmap.json
```

**⑤ 課題に紐づく計画の葉**（`links[].wbs` の id を `tasks` から再帰で探す。親子ネストの深さを知らなくてよい）

```bash
# どの課題がどの葉に紐づいているか
jq -r '.projects[] | .issues[]? | select((.links//[])|any(.wbs)) | "#\(.id) \((.links[]|select(.wbs)|.wbs))"' wbs_roadmap.json
# その id の葉を1つ引く
jq -r --arg w "4.14" '.projects[] | select(.name=="single-file-wbs 開発")
       | [.tasks[]? | .. | objects | select(.id==$w)] | .[]
       | "\(.id)\t\(.name)\t予定 \(.plan.start//"—")〜\(.plan.end//"—")\t実績 \(.actual.start//"—")〜\(.actual.end//"—")"' wbs_roadmap.json
```

**⑥ 計画の葉で、まだ着手していないもの**（`plan` を持つ＝葉。`actual.start` が空）

```bash
jq -r '.projects[] | .name as $p
       | (.tasks[]? | .. | objects | select(has("plan") and (.actual.start//null)==null) | "\($p)\t\(.id)\t\(.name)")' wbs_roadmap.json
```

- `.issues[]?` と `.tasks[]?` の **`?`** は「そのキーが無い案件では黙って飛ばす」印。計画だけの案件・課題だけの案件が混ざっても落ちない。
- `.. | objects` は**入れ子の深さを問わず全オブジェクトを舐める**書き方。WBS の親子が何階層でも同じ式で拾える。
- **葉の見分けは `has("plan")`**（`plan` を持つのが葉・持たないのが集計ノード）。
- **jq は読むだけ**。`jq` の出力でファイルを上書きしない（キーの順番とカスタムキーが崩れる）。**書き込みは JSON を開いて必要な箇所だけ直す。**

---

## AI への依頼例（課題側・原則 JSON を編集させる）

> 計画（`tasks`）側の依頼例は →「計画（`tasks`）の追加・更新」。以下の before/after は**変化するキーだけの断片**。id は `wbs_sample_issues.json` の課題に合わせてある（`#1`＝対応中の移行エラー、`#2`＝未決が残る帳票、`#3`＝待ちの仕様回答、`#4`＝発生せずで閉じた行、`#6`＝凍結中の夜間バッチ、`#9`＝重複で閉じた行）。`#3`／`#4`／`#6`／`#9` は**依頼を実行した後の姿**でサンプルに入っている。

### ① 載せる（起票）

> 「移行テストで文字コードのエラーが大量に出た。放置すると本番切替で同じことが起きて移行が失敗する。リハーサルでエラーが出ないことを完了条件にして載せて」

`issues[]` の末尾に1件追加（`id` ＝ 既存の最大+1）：

```json
{
  "id": 10,
  "title": "移行テストで文字コード起因のエラーが大量発生",
  "priority": "high",
  "opened": "2026-09-05",
  "due": "2026-10-20",
  "ifIgnored": "本番の切替で同じエラーが起き、移行が失敗する。影響が出るのは本番切替日",
  "ifDone": "",
  "closeWhen": "リハーサルで一連の移行手順を通して、エラーが出ないこと",
  "decisions": [],
  "pending": null,
  "closed": null,
  "links": [],
  "actions": [ { "date": "2026-09-05", "text": "事象発生", "assignee": "", "done": true } ],
  "note": ""
}
```

- 二つの問いのどちらにも答えがないときは**載せずに聞き返す**。聞き方は「**放置すると、いつ・何が起きますか**（例：10/20 の本番切替で帳票が読めなくなる）」「**やると、いつ・何が得られますか**（例：11月から、毎月の締め作業が半日で終わる）」。
- `due` は「支障や価値が現れる日」。分からなければ聞く（適当な日付を入れない）。

### ② 待ちにする（相手の返事や物を待っている）

> 「#3 を待ちにして。開発部の仕様回答、9/12 まで」

before → after（`pending` のみ）：

```json
"pending": null
```
```json
"pending": {
  "kind": "waiting",
  "since": "2026-09-06",
  "who": "開発部",
  "until": "2026-09-12",
  "what": "文字コード変換の仕様（外字と機種依存文字の扱い）の回答"
}
```

- **`who`（誰を）と `what`（何を）は空にしない**。書いていない待ちは、誰も催促できないまま止まり続ける。
- **`until`（いつまで）を入れると催促が出る**：本日がその日を過ぎると `⚠ 催促` の印が付く。期限を切っていなければ `null` でよいが、そのぶん催促は出ない。
- **「決められる人の判断待ち」は待ちではない** → `decisions[]` の未決項目（`decided: null`）にする（→ 依頼例④-2）。
- `decisions` は触らない。方針が決まっていても、物や返事を待って止まることはある。
- 解除は `"pending": null` に戻す。

### ②-2 凍結する（意図的に止める）

> 「#6 を凍結。本番切替が終わってから再開する。いまは移行を優先」

```json
"pending": null
```
```json
"pending": {
  "kind": "frozen",
  "since": "2026-08-27",
  "resumeWhen": "本番切替が完了したら再開する",
  "detail": "移行作業を優先するため、意図的に止めている"
}
```

- **`resumeWhen`（再開条件）を空にしない**。書かない凍結は、そのまま忘れられる。
- 凍結に `until` は無い（**催促する相手がいない**＝自分で決めて止めているから）。
- 解除は `"pending": null`。

### ②-3 課題が終わるのを待つ

> 「#5 は勤怠の #2 が終わるまで待ち」

**待っている相手が人ではなく課題**のときは、`who` に**課題参照**を書く（文字列で名前を書かない）：

```json
"pending": null
```
```json
"pending": {
  "kind": "waiting",
  "since": "2026-09-09",
  "who": { "project": "勤怠システム更改", "issue": 2 },
  "until": null,
  "what": "日跨ぎの変換が片づくまで、検証の並列化には着手できない"
}
```

- **同じ案件なら `{ "issue": 2 }`**、別案件は `{ "project": "…", "issue": 2 }`（`links` と同じ書き方）。
- **相手の課題が完了したら `⚠ 催促` が出る**。「待つ理由は消えたのに、まだ待ちのまま」という合図なので、解除して次の行動を足す。
- `until` は入れなくてよい（相手の課題の期限がそのまま効くから）。入れれば期限超過で催促も出る。
- **同じ相手を `links` に重ねて書かない**。待ちの行そのものが画面でリンクになるので、`links` にも同じ課題を足すと**同じ行き先が2つ並ぶ**。`links` に書くのは「待ち」以外の関係（関連・重複元・WBS・外部URL）だけ。
- 存在しない課題を指すと**壊れたリンク**として薄い取り消し線が出る（検査スクリプトはエラー）。

### ②-4 催促が必要なものを挙げる（待ち・未決）

> 「催促が必要なものを挙げて」

**読むだけ**。JSON は変えない。**待ちと未決の両方**を拾う（`⚠ 催促` は同じ印）：

```
待ち
  勤怠システム更改 #2 打刻データの移行で深夜勤務の日跨ぎが崩れる
    開発ベンダー に「日付境界の仕様書の差し替え版」を 9/8 まで（1日超過）
未決
  販売管理システム移行 #2 手書き帳票を移行対象に含めるか（業務部の判断待ち）
    9/4 までに決めたかった（5日超過・16日止まっている）
```

- 拾う条件は3つ：**待ちで `pending.until` 超過**／**未決で `decisions[].until` 超過**／**待ちの相手の課題が完了**。
- `until` が `null` のものは催促の対象外。件数だけ「期限なしの待ち N 件・未決 N 件」と添えると親切。
- 完了した課題は出さない（もう追いかけない）。
- **1件も無ければ「催促が必要なものはありません」と書く**（黙って空で返さない）。

### ③ 解決で閉じる

> 「#1 を解決で閉じて。リハーサルでエラー0件だった」

before → after（`closed` のみ）：

```json
"closed": null
```
```json
"closed": {
  "at": "2026-09-20",
  "how": "resolved",
  "note": "第1回リハーサルで全手順を通し、変換エラー0件を確認した"
}
```

- `note` は **`closeWhen` と対応させる**。`closeWhen` が「エラーが出ないこと」なら、`note` は「エラー0件を確認した」という**確かめた事実**を書く。「対応した」だけでは閉じる根拠にならない。
- `pending` が残っていたら `null` に戻す。

### ④ 「対応しない」と決めて閉じる（2か所を書く）

> 「#2 の方針は『対応しない』で決めて閉じて。手書き帳票は業務側で運用を変えることになった」

**未決だった項目を決めたことにし、同時に閉じる**（片方だけだと「なぜやらないと決めたか」が残らない）：

```json
"decisions": [
  { "q": "手書き帳票を移行対象に含めるか（業務部の判断待ち）", "since": "2026-08-24",
    "decided": null, "a": "" }
],
"closed": null
```
```json
"decisions": [
  { "q": "手書き帳票を移行対象に含めるか（業務部の判断待ち）", "since": "2026-08-24",
    "decided": "2026-09-09", "a": "対応しない。手書き帳票は業務側で運用を変える" }
],
"closed": { "at": "2026-09-09", "how": "wontfix", "note": "業務部と合意。移行対象一覧から除外した" }
```

- **未決の項目があるなら、新しく足さずにその項目を決める**（同じ問いを2回積まない）。
- 未決の項目が無ければ `decisions` に1件足す（`q`＝決めたこと、`since`＝今日、`decided`＝今日、`a`＝方針）。

### ④-2 決めるべきことを未決として積む

> 「#1 に『変換ツールを内製するか、既存のライブラリを使うか』を未決として載せて、9/16 までに」

`decisions[]` の末尾に **`decided: null`** の項目を1件足す：

```json
"decisions": [
  { "q": "対応方針", "since": "2026-09-05",
    "decided": "2026-09-07", "a": "原因調査と変換処理の修正で対応する" }
]
```
```json
"decisions": [
  { "q": "対応方針", "since": "2026-09-05",
    "decided": "2026-09-07", "a": "原因調査と変換処理の修正で対応する" },
  { "q": "変換ツールを内製するか、既存の変換ライブラリを使うか", "since": "2026-09-09",
    "until": "2026-09-16", "decided": null, "a": "" }
]
```

- 「**9/16 までに**」のように**期限を言われたら `until` に入れる**。過ぎると `⚠ 催促` が出る（→ 依頼例②-4）。言われなければ `until` は省く（`null`）。
- `since` は**今日**（「いつから決まっていないか」を測る起点。画面はここからの経過日数を出す）。
- **決めていないのに `a` を埋めない**。埋めるのは `decided` を入れる時だけ。
- **課題を分けなくてよい**。1つの課題に決めるべきことが3つあっても、3件積めばいい。
- **状態は変わらない**（未決は印であって状態ではない）。

### ④-3 未決だったことが決まった

> 「#1 の『変換ツールを内製するか』は、既存のライブラリを使うで決まった」

その項目に **`decided`（決めた日）と `a`（方針）** を入れる。項目は増やさない：

```json
{ "q": "変換ツールを内製するか、既存の変換ライブラリを使うか", "since": "2026-09-09",
  "decided": null, "a": "" }
```
```json
{ "q": "変換ツールを内製するか、既存の変換ライブラリを使うか", "since": "2026-09-09",
  "decided": "2026-09-09", "a": "既存の変換ライブラリを使う（内製はしない）" }
```

- `q` と `since` は**書き換えない**（いつから決まっていなかったかの記録）。
- 決まった項目は帯から消え、概要に **`方針：既存の変換ライブラリを使う（9/9）`** として並ぶ。
- **決めた日が更新期間の中なら ★ が付く**（→「報告する」節）。
- 方針を**変えた**ときは、その項目の `a` を書き換え `decided` を新しい日にする。前の方針を残したければ `note` に1行添える（履歴のキーは持たない）。

### ④-4 未決を洗い出す

> 「未決を洗い出して。長く止まっている順で」

**読むだけ**。JSON は変えない。全案件の `decisions` から **`decided: null`** の項目を集め、**`since` からの経過日数の長い順**に並べる：

```
20日 勤怠システム更改 #1 丸め規則を現行のまま残すか、法令基準に直すか（人事部の判断待ち）
16日 販売管理システム移行 #2 手書き帳票を移行対象に含めるか（業務部の判断待ち）
12日 販売管理システム移行 #5 検証環境をもう1面用意するか（PLの判断待ち）
 2日 販売管理システム移行 #1 変換ツールを内製するか、既存の変換ライブラリを使うか
 2日 販売管理システム移行 #1 旧システムの外字を代替文字にするか、個別に対応するか
 2日 勤怠システム更改 #4 繰越の丸め方を旧に合わせるか、法令基準に直すか
```

- 数えるのは**項目**であって課題ではない（1つの課題に複数あってよい）。
- 完了した課題の未決は出さない（閉じたものは追いかけない）。
- **長く止まっているものほど上**。「決められる人に持ち込む」ための一覧なので、`q` に「（誰の判断待ち）」が書いてあると持ち込み先が分かる。

### ⑤ 次の行動を追加する

> 「#1 の次の行動を 9/21 に追加。リハーサル結果のレビュー、担当は Aさん」

`actions[]` の末尾に1件追加：

```json
{ "date": "2026-09-21", "text": "リハーサル結果のレビュー", "assignee": "Aさん", "done": false }
```

- 日付が決まっていなければ `"date": null`（＝調整中）。**架空の日付で埋めない。**
- 済みにするのは `"done": true`。日付が**報告期間（`star`）の中**なら、その行に自動で ★ が付く（★は手で書かない）。

### ⑥ 発生せずで閉じる

> 「期限切れで何も起きなかった #4 を発生せずで閉じて」

```json
"closed": null
```
```json
"closed": {
  "at": "2026-08-31",
  "how": "not_occurred",
  "note": "8/31 の月末バッチは定刻に完了。リハーサル枠とも重ならなかった"
}
```

- **行は消さない。**「心配したが起きなかった」も記録として価値がある。
- 期限を過ぎただけで影響がまだ判断できないなら、閉じずに `due` を見直す。

### ⑥-2 重複で閉じる

> 「#9 は #1 と同じ話なので重複で閉じて」

**`closed` と `links` を両方書く**（元の課題へたどれないと、閉じた行が行き止まりになる）：

```json
"closed": null,
"links": []
```
```json
"closed": { "at": "2026-09-09", "how": "duplicate", "note": "#1 と同じ文字コードの話。以後は #1 で追う" },
"links": [ { "issue": 1 } ]
```

- **重複かどうかは「完了条件（`closeWhen`）が同じか」で決める**。タイトルに同じ言葉（例：「文字コード」）が入っていても、**確かめたい状態が違えば別件**。その場合は重複で閉じず、`links` の `{ "issue": N }` で**関連として結ぶだけ**にする。
- **`links` は必須**（`{ "issue": N }`／別案件は `{ "project": "…", "issue": N }`）。無いと検査スクリプトがエラーにする。
- **残すのは新しく載せた方ではなく、話が進んでいる方**。行動や方針が溜まっている側を生かし、もう片方を重複で閉じる。
- 閉じる側に有用な行動・未決が残っていたら、**生かす側へ移してから**閉じる（消さない）。
- **閉じる課題が他から指されていないか確かめる**：別の課題の `links` や `pending.who` がこの課題を指していたら、**指す側を「残す課題」へ付け替えてから**閉じる（重複で閉じた行き止まりを待ち続けることになる）。付け替え漏れは `uv run python scripts/check.py` では拾えない（参照自体は壊れていない）ので、**閉じる前に指している側を探す**。
- これは「起票時のチェック」4（**同じものが既に載っていないか**）の受け皿。載せる前に気づけば起票せず、載せた後に気づいたらこれで閉じる。

### ⑦ 計画（WBS）に組み込んだと記録する

> 「#3 を WBS の 2.3 に紐づけて」

課題の `links[]` に1件足す（**`actions[].wbs` は使わない＝廃止**）：

```json
"links": [ { "issue": 1 } ]
```
```json
"links": [ { "issue": 1 }, { "wbs": "2.3" } ]
```

- 相手は**同じ案件の `tasks` にある id**。無い id を書くと画面で**壊れたリンク**（薄い取り消し線）になる。
- **WBS の葉＝この課題1件**。工数・予定・実績は計画側に書き、課題側に転記しない。課題の中の手順は `actions[]` のまま。
- 計画側には何も書かない。計画の行に出る `課題 #3` の札は、`links` から**逆引きして描いている**だけ。

### ⑦-2 別の課題に紐づける

> 「勤怠システム更改の #2 を、関連課題として #3 に紐づけて」

`#3`（今開いている案件の課題）の `links[]` に、**別案件を指す形**で足す：

```json
"links": [ { "wbs": "2.3" } ]
```
```json
"links": [ { "wbs": "2.3" }, { "project": "勤怠システム更改", "issue": 2 } ]
```

- **同じ案件なら `{ "issue": 2 }`**、別案件なら `{ "project": "…", "issue": 2 }`。鍵の名前で区別する。
- リンクは**片側だけ**でよい（画面はどちらからも見えるようにしない＝データを二重に持たない）。相手側からもたどりたいときだけ、相手にも足す。
- **案件名を変えたら、その名前を指しているリンクも直す**（名前が唯一の手がかり）。

### ⑦-3 リンクが壊れていないか検査する

> 「リンクが壊れていないか検査して」

**読むだけ**では分からないので、同梱の検査スクリプトを走らせる：

```
uv run python scripts/check.py wbs.json
# uv が無い環境では
python3 scripts/check.py wbs.json
```

- 見るもの：リンク先の存在（案件名・課題番号・タスク id）／`links` の形／番号の重複／日付・enum／待ちの相手抜け・凍結の再開条件抜け／旧キー（`sheets`・課題レベル `assignee`・`actions[].wbs`）の残存。
- **エラーがあれば終了コード 1**。`--quiet` を付けると件数の要約だけ出る。
- 指摘は行番号ではなく **`案件名 / #番号 / 項目`** で出るので、そのまま直しに行ける。

### ⑦-4 組み込む先の葉がまだ無い場合

> 「#2 を組み込んで」（対応する WBS の葉がまだ無い）

**計画に葉を1つ足してから、課題の `links` で結ぶ。**まず `qty`／`hours`／`plan` を利用者に聞く（勝手に決めない）。

案件の `tasks` に葉を1つ追加（`id` は**案件の中で重複しない採番**・`name` は課題タイトル）：

```json
{ "id": "2", "name": "変換ツール", "children": [
  { "id": "2.3", "name": "文字コード変換", "qty": 1, "hours": 16, "assignee": "ぴぐお",
    "plan": { "start": "2026-09-14", "end": "2026-09-18" },
    "actual": { "start": null, "end": null }, "note": "" }
] }
```
```json
{ "id": "2", "name": "変換ツール", "children": [
  { "id": "2.3", "name": "文字コード変換", "qty": 1, "hours": 16, "assignee": "ぴぐお",
    "plan": { "start": "2026-09-14", "end": "2026-09-18" },
    "actual": { "start": null, "end": null }, "note": "" },
  { "id": "2.5", "name": "旧システムの手書き帳票を移行対象に含めるか", "qty": 1, "hours": 8,
    "assignee": "佐藤",
    "plan": { "start": "2026-09-24", "end": "2026-09-25" },
    "actual": { "start": null, "end": null }, "note": "" }
] }
```

続けて **課題 `#2` の `links` に足す**（葉を足しっぱなしにしない）：

```json
"links": []
```
```json
"links": [ { "wbs": "2.5" } ]
```

- **案件に `tasks` が無ければ**、`tasks: []` を新設して**葉を直置き**する（親を作らなくてよい。`id` は `"1"` から順に）。
- 足すのは**葉1つだけ**。課題の中の手順は `actions[]` のままで、WBS に子は作らない。
- **`qty` は既定 1**（数量の考え方が要らない作業がほとんど）。実際に聞くのは **`hours` と `plan`** だけでよい。
- 葉の **`name` は課題タイトルそのままでよい**。計画側の「全角14字目安」を超えても構わない（一覧で見切れるだけ・全文はホバーで出る）。短くしたければ短くして、詳細は課題側からリンクで辿る。
- `hours`／`plan` が分からないまま**仮の数字を入れない**（→「依頼が曖昧・矛盾する時の作法」12）。

### ⑧ 優先度を見直す

> 「優先度を影響の大きさ×期限の近さで見直して」

各課題の `ifIgnored`／`ifDone`（影響の大きさ）と `due`（その日の近さ）を読み、`priority` を書き換える：

```json
"priority": "mid"
```
```json
"priority": "high"
```

- 書き換えるのは `priority` の値だけ。**計算式やスコアを新しいキーに保存しない**（`priority` は人／AI の判断値）。
- 変えた理由は `note` に1行足すか、会話で返す。
- 完了した課題（`closed` あり）の優先度は触らない。

### ⑨ 今回の★を報告する

> 「今回の★を箇条書きで報告して」

**読むだけ**。JSON は変えない。`star` の期間（無ければ直近7日）で★の判定規則に当たる課題を拾い、課題ごとにまとめる：

```
#1 移行テストで文字コード起因のエラーが大量発生
   9/5 事象発生 → 9/7 方針を決定（原因調査と変換処理の修正で対応する）
#3 文字コード変換の仕様が開発部から未回答
   9/5 質問票を送付・方針を決定 → 9/6 待ちにした（開発部の回答・9/12 まで）
#7 移行ツールのログを画面から検索できるようにする
   9/5 「対応しない」と決めて完了
```

- 拾い漏れを防ぐため、**行動だけでなく `decisions[].decided`／`pending.since`／`closed.at` も見る**。
- 期間外の動きは書かない。★が無い課題は報告に出さない（聞かれたら別途答える）。
- **1件も無ければ「この期間に動いた課題はありません」と書く**（黙って空で返さない）。
- **末尾に「注意」を1〜3行添える**：`⚠ 催促`（待ち・未決）／`⚠ 期限超過`／長く止まっている未決。★は「動いたもの」なので、**動いていないが手を打つべきもの**は★に出てこない。例：

```
注意
  催促 2（勤怠 #2 の仕様書が 1日超過／販売管理 #2 の未決が 5日超過）
  期限超過 2（販売管理 #2・勤怠 #1）
  20日 止まっている未決 1（勤怠 #1 丸め規則）
```

### ⑩ 更新期間を次に進める

> 「定例が終わったので更新期間を 9/8〜9/14 に進めて」

トップレベルの `star` を書き換える：

```json
"star": { "from": "2026-09-01", "to": "2026-09-07" }
```
```json
"star": { "from": "2026-09-08", "to": "2026-09-14" }
```

- **「次の週にして」と言われたら**：`from` を**いまの `to` の翌日**（9/7 → 9/8）、`to` を**`from` + 6日**（9/8 → 9/14）にする。期間は**両端を含む**ので 7 日ぶんになる。
- `to` を省くと**本日まで**になる（`{ "from": "2026-09-08" }`）。次の定例までの途中経過を見たいときはこちら。
- 書き換えてよいのは `star` だけ。**★そのもの（どの行に付くか）はデータに書かない**。

### ⑪ 案件を追加する

> 「勤怠システム更改の案件を足して」

`projects` 配列の末尾に1件追加する（中身は空でよい）：

```json
{ "name": "勤怠システム更改", "issues": [] }
```

- **案件名はブックの中で一意に**する（別案件からのリンクが名前で指すため）。
- 案件は**仕事の仕切り**。種別（障害／備忘／改善）で分けない（1つの表に混ぜるのが本来）。
- 報告の単位が違う（更新期間を別々にしたい）なら、案件ではなく**ファイルを分ける**。
- 計画（`tasks`）だけの案件、課題（`issues`）だけの案件、両方ある案件、どれも有効。

### ⑫ サマリを読んで報告する

> 「サマリを見て、今週動いた案件を挙げて」

**読むだけ**。JSON は変えない。案件ごとに `★ 更新` と `⚠ 期限超過` を数えて答える：

```
勤怠システム更改   ★ 更新 1（#2 日跨ぎの変換、9/2 に方針決定）／⚠ 期限超過 1（#1 未決が1件残ったまま 9/3 超過）
販売管理システム移行 ★ 更新 3（#1 調査、#3 待ちにした、#7 対応しないで完了）／⚠ 期限超過 1
```

- 「今週」の範囲は**更新期間**（`star`。無ければ直近7日）。**案件をまたいで**数える。
- ★が0の案件は「今週は動いていない」と書く（隠さない）。
- **遅れが0件なら「遅れているタスクはありません」**、**案件に `tasks` が無ければ「計画（WBS）が無いため判定できません」**と明記する（黙って省かない）。
- **末尾に「注意」を添える**（⑨と同じ）：案件をまたいだ `⚠ 催促`・`⚠ 期限超過`・長く止まっている未決の件数。動いた話だけで報告を終えない。

### ⑬ 課題を別の案件へ移す

> 「#3 を勤怠システム更改の案件へ移して」

元の案件の `issues` から取り除き、移動先の `issues` の末尾に足す。このとき **`id` は移動先で振り直す**（`id` は案件の中で一意）：

```json
"id": 3
```
```json
"id": 4
```

- 振り直すのは移動のときだけ。**同じ案件の中では `id` を振り直さない**（過去の議事録やチャットが番号で指しているため）。
- **その課題を指していたリンクも直す**：同じ案件だった `{ "issue": 3 }` は、移動後は `{ "project": "勤怠システム更改", "issue": 4 }` になる。移した課題自身が持つ `{ "wbs": … }` も、移動先の案件に同じタスクが無ければ外す。
- 移し終えたら `uv run python scripts/check.py`（または `python3 scripts/check.py`）で壊れたリンクが無いか確かめる。
- `note` に「販売管理システム移行から移動」と1行残すと、番号が変わった理由が追える。

### ⑭ 課題に紐づく計画の予定を見せる

> 「#3 の計画（WBS 2.3）の予定を見せて」

**読むだけ**。JSON は変えない。課題の `links` から `{ "wbs": … }` を拾い、**同じ案件の `tasks`** をたどって該当の葉の `plan`／`actual` を読む：

```
#3 文字コード変換の仕様が開発部から未回答
  WBS 2.3 文字コード変換（担当 ぴぐお・16h）
    予定 2026-09-14 〜 2026-09-18
    実績 未着手
```

- 読むのは**計画側の値**（`plan.start`／`plan.end`／`actual.start`／`actual.end`／`qty`／`hours`／`assignee`）。**課題側に転記しない**。
- 工数の式・進捗の出し方は →「計算ロジック」。ここで独自に計算しない。
- `links` に `{ "wbs": … }` が無ければ「まだ計画に組み込まれていません」と答える（勝手に組み込まない → 依頼例⑦-4）。

### ⑮ 遅れているタスクに紐づく、未決の残っている課題を挙げる

> 「遅れているタスクに紐づく、未決の残っている課題を挙げて」

**読むだけ**。2段構えで拾う：

1. **計画側で遅れを判定**する。判定規則は →「計算ロジック」（期限の遅れ＝**本日 > `plan.end` かつ未完了**〈`actual.end` が無い〉／進捗の遅れ＝`PV > EV`）。ここでは規則を作り直さない。
2. 遅れている葉の `id` を、各課題の `links` の `{ "wbs": … }` と突き合わせ、**未決が残っている課題**（`decisions` に `decided: null` の項目がある）だけを残す。

`wbs_sample_issues.json` を本日 2026-09-07 で見ると、遅れている葉は無いので**該当0件**になる：

```
遅れているタスクはありません（販売管理システム移行の予定終了は 9/18・10/2・10/20 でいずれも未到来）。
勤怠システム更改：計画（WBS）が無いため判定できません。

参考：計画に紐づいていないが、未決が残ったまま期限を過ぎている課題
  販売管理システム移行 #2 旧システムの手書き帳票を移行対象に含めるか（期限 9/4）
  勤怠システム更改   #1 打刻の丸め規則を現行のまま残すか（期限 9/3）
```

- **この例の数値はサンプル基準**。実データでは**必ず本日の日付で再計算**する（例を写さない）。
- **該当0件なら「遅れているタスクはありません」と明記**、**案件に `tasks` が無ければ「計画（WBS）が無いため判定できません」と明記**する（黙って省かない）。
- **「未決」は状態ではなく印**。`decisions` に `decided: null` の項目が1件でもあれば「未決あり」（状態は 未着手／対応中／完了 のどれか）。
- **1つの JSON に計画と課題があるから、これが1回の読み取りで済む**（ファイルをまたがない）。
- 状態の条件を変えて頼まれたら（「待ちも入れて」など）そのまま従う。**JSON は変えない**（→「依頼が曖昧・矛盾する時の作法」11）。

### ⑯ 計画のタスクが終わったと記録する

> 「2.4 終わった」

**計画側の実績を書く**（課題ツールが `tasks` を書き換える2つ目の例外）。既定は**本日**：

```json
{ "id": "2.4", "name": "移行リハーサル", "qty": 1, "hours": 24, "assignee": "ぴぐお/Aさん",
  "plan": { "start": "2026-09-24", "end": "2026-10-02" },
  "actual": { "start": null, "end": null }, "note": "" }
```
```json
{ "id": "2.4", "name": "移行リハーサル", "qty": 1, "hours": 24, "assignee": "ぴぐお/Aさん",
  "plan": { "start": "2026-09-24", "end": "2026-10-02" },
  "actual": { "start": "2026-09-07", "end": "2026-09-07" }, "note": "" }
```

- **`actual.end` に本日**。**`actual.start` が空なら、そこにも本日**を入れる（計画側の完了トグルと同じ挙動＝着手日の無い完了行を作らない）。
- **予定開始より前でも止まらない**。「予定（9/24 開始）より早い完了として記録しました」と**一言添えて進める**。
- **実績開始日を教えてもらえればそれを使う**（「9/2 に着手してた」→ `actual.start` は `2026-09-02`）。
- 書き換えるのは `actual` だけ。**`plan` は動かさない**（予定変更は別の依頼＝リスケ。`_planLog` に1行残す → 「計画（`tasks`）のフィールド定義」）。
- 進捗率・工数・イナズマ線は**自動計算**なので触らない（計算の仕様は →「計算ロジック」）。

### そのほかよくある依頼

- 「未決が残ったまま期限を過ぎている課題を挙げて」→ **読むだけ**（`decisions` に `decided: null` があり、`due` < 本日、`closed: null`）。JSON は変えない。
- 「待っている相手が書かれていない行を探して」→ `pending.kind` が `"waiting"` で `who` か `what` が空の行。
- 「催促が必要な待ちを挙げて」→ **読むだけ**（`kind: "waiting"` かつ `until` < 本日）。誰に・何を・何日過ぎたかを添える。
- 「この課題を再オープンして」→ `"closed": null` に戻す。
- 「#1 の次の行動の担当を佐藤に変えて」→ 該当する `actions[].assignee` を書き換える。**課題に担当欄は無い**ので、たたんだ時の詳細列（次の一手1行）の `担当：` も自動で変わる。

---

## 異常系（壊れた入力の扱い）

**方針：壊れた入力でもクラッシュさせない（graceful degradation）。** 無効値は無視・0・空で表示する。
データ投入時は下記を避ける（入れても落ちないが表示は崩れる）。

**共通**

| 入力（異常） | ビューアの扱い |
|---|---|
| 不正な日付（`YYYY-MM-DD` 以外・**1900〜2099年の範囲外**） | **無視**（`plan`／`actual`／`due`／マイルストーン／祝日 共通）。日付欄は `—` |
| `projects` が非配列／空／要素が非オブジェクト | **変換するのは計画だけの旧形式 `{ project, tasks }` だけ**。**課題側の旧形式（`sheets`・トップレベルの `issues`）は読まない**（→「後方互換の約束」）ので、その形のファイルは**空表示**になる |
| `projects[].name` 欠損・重複 | 名前なしのタブとして描く。**重複するとリンクが指せない**ので検査スクリプトがエラーにする |
| 同一案件内の **重複 `id`** | 折りたたみキーが衝突（同 id が同時に開閉） |
| トップが `null`・数値等の非オブジェクト | 空表示（クラッシュしない） |
| 不正な JSON（D&D／ファイル選択／更新） | アラートで通知（無言失敗しない） |
| `<script>` 等を含む文字列 | すべてエスケープして表示（属性へ生値を流さない） |
| `holidays` が非配列・要素が不正日付 | **無視**（赤字/列ピンクにしない・クラッシュしない） |
| 範囲外・色なし・不正日付のマイルストーン | **描画されない**（無視） |
| マイルストーンの `label` 欠損 | **空ラベルで描画**（クラッシュしない） |
| マイルストーンの `color` が `#hex` 以外 | **無視してデフォルト色**（属性インジェクション防止） |

**計画（`tasks`）**

| 入力（異常） | ビューアの扱い |
|---|---|
| `tasks: []`（空） | 空表示（クラッシュしない） |
| 進行中なのに `plan.end` 欠損 | 進捗 **0%**（NaN を回避） |
| 終了 < 開始（期間の逆転） | バーが描画されない／異常な見た目 |
| 実績開始が未来日 | 進捗 0 |
| `qty` / `hours` 欠損・0 | 工数 0（空表示） |
| 少数の `qty`（例 0.5） | 小数の人日（1.5 等） |
| 4 階層を超えるネスト | 色は **3L 止まり**（崩れはしない） |
| `_progress` が数値以外 | **無視**（時間ベースの進捗にフォールバック）。数値なら最寄り10%に丸め **0〜100 に clamp** |
| `_planLog` の要素が壊れている | その要素だけ**無視**（他の履歴は出す） |

**課題（`issues`）**

| 入力（異常） | ビューアの扱い |
|---|---|
| `issues` が空 | 空表示（クラッシュしない） |
| `priority` が3値以外 | `"mid"`（中）として扱う |
| `pending.kind` が既知値以外 | 印を出さない（`pending` は無いものとして描く）。検査スクリプトはエラーにする |
| `pending.until` が不正日付 | **期限なしの待ち**として扱う（催促は出さない） |
| `closed.how` が既知値以外 | サブ表示無しの「完了」 |
| 課題レベルの `assignee`（旧データ） | **読まない・無視**（担当は `actions[].assignee` のみ。担当列は存在しない）。保存では消さない |
| `decisions` が非配列／要素が非オブジェクト | その要素だけ**無視**（他の項目は出す）。検査スクリプトはエラーにする |
| `decisions[].q` が空 | **表示モードでは**帯にも方針行にも出さない（`q` の無い項目は意味を持てない）。**編集モードでは出す**（`＋方針` で足した空の1件をその場で書けるように）。**未決 N の数え方は変わらない**（事実のまま数える） |
| `decisions[].decided` が不正日付 | **未決として扱う**（日付は `—`） |
| `links` が非配列／4形以外の要素 | その要素だけ**無視**（他のリンクは出す）。検査スクリプトはエラーにする |
| `links` の指す先が無い（タスク・課題・案件） | **薄い取り消し線＋ツールチップ**。消さない・落ちない |
| `links` の `url` が `http(s)` 以外 | **壊れたリンクとして描く**（薄い取り消し線＋ツールチップ・**クリックできない**）。属性へ生値を流さない |
| `star` が不正（オブジェクトでない・日付が不正・`from` > `to`） | **無視して既定の直近7日**（本日を含む） |
| `actions` が非配列／要素が非オブジェクト | **無視** |

- 確認用データは **計画側＝`tests/正常_*.json`／`tests/異常_*.json`**（一覧は `tests/INDEX.md`）、**課題側＝`tests/issue/正常_*.json`／`tests/issue/異常_*.json`**（一覧は `tests/issue/INDEX.md`）。
- 「クラッシュしないこと」は headless Chromium で全テストを読み込み、**JS エラー・`NaN` 無し**を確認済み。

---

## 既知の制限

- **大量行（数千〜）で初期描画が重くなる**（毎回まとめて再構築）。対策＝折りたたみ＋アーカイブ運用（別ファイルへ退避）。本当に重くなったら仮想化が必要。
- **`id` の重複は折りたたみ状態を共有する**（同時に開閉する）。`id` は案件の中で一意に。**同名の案件**も折りたたみキーが衝突するので、案件名は一意に。
- **配列内の `null` 要素（null のタスク/案件）は非対応**（トップの非オブジェクトは空表示で吸収）。
- **待ち・凍結の履歴を持たない**。`pending` は今の1件だけで、いつ何回止まったかは残らない（残したければ `_` キーに自分で書く）。**予定変更の履歴（`_planLog`）は計画側にだけある。**
- **課題どうしの依存関係を持たない**（「#3 が終わらないと #1 が進まない」は `note` に書く）。計画側にも依存線（先行/後続）は無い。
- **課題 → 計画も、課題 → 課題も、同じページの中の移動**（`#wbs=`／`#issue=` を URL に書く）。**別タブで開くのは外部URL（`{ title, url }`）だけ**。
- **リンクは片側だけ**。`{ "issue": 2 }` を書いても、相手の課題に自動では現れない（両方からたどりたいなら両方に書く）。**計画 → 課題は例外**で、画面が `links` から逆引きして札を出す（データは片側のまま）。
- **案件名を変えると、その名前を指すリンクが切れる**（不変のキーは持たない）。変えたら `scripts/check.py` で確かめる。編集モードで案件名や `id` を変えると**折りたたみ状態もリセット**される（折りたたみキーが名前/id 由来のため）。
- **複数行の `note` は計画側の編集モードで触ると1行に平坦化**される（input が改行を保持できない）。課題側の `note` は textarea なので改行を保つ。
- **キーボード操作・スクリーンリーダー対応なし**（マウス前提の個人ツール）。

## メモ

- Chromium 系（Chrome 推奨）・`file://` 前提（File System Access API を使用）。
- 自分の実データ `wbs.json` は**既定で gitignore 済み**（誤コミット防止）。`wbs-archive-*.json` も無視。
- サンプルは2つ。**`wbs_sample.json`**＝計画（`tasks`）だけの従来のサンプル／**`wbs_sample_issues.json`**＝計画と課題のサンプル（架空・**2案件のブック**。1件目は**計画と課題が同居**する実例・**3状態と6つの印を網羅**）。このリポジトリ自身の正本は末尾の「このリポの正本」節。
- 公開リポなので、サンプル・スクショには**架空名のみ**。実在の会社名・案件名・個人情報・単価は入れない。

---

## 開発運用：`/pm` スキル（課題 × 計画のメンテ）— 完全リファレンス

このツール自身の開発（管理資料）を AI で一貫維持するスキル。**本節がその単一ソース**
（利用者側の `~/.claude/commands/pm.md` が本節を読む。手元では `~/.claude/CLAUDE-single-file-wbs.md` が本ファイルへの symlink になっている。本リポは `.claude/` を gitignore しているので、コマンド定義はリポジトリに置かない）。
会話/指示の**文脈からモードを判断**して適用する（`/pm` を明示しなくてよい）。判断は人間・進行と記録はスキル。

**正本は、この統合ビューアが読む JSON（`projects[].issues` と `projects[].tasks`）**。
課題も開発計画も同じ1ファイルに載せ、**GitHub Issue と二重に持たない**（どちらが本当か分からなくなる）。
どのファイルを正本にするかは**リポジトリごとに1つだけ**決めて、そのリポの `CLAUDE.md` に宣言する。

### 真実の分担
- **課題（`issues[]`）＝ 何を・なぜ・どうなったら終わりか**／**計画の葉（`tasks` のリーフ）＝ いつ・誰が・どれだけ**。繋ぐ鍵は **課題の `links[].wbs`**（→「課題管理の4つの動き ⇄ JSON 操作」の末尾が正）。
- **1課題＝1葉**。課題の中の手順は**課題の `actions[]`** に書き、**WBS に子は作らない**（→「AI が迷わないためのルール」の1行目）。
- 形式は本ファイル上部の「データ（`wbs.json`）」「計画（`tasks`）の追加・更新」「計算ロジック」節が正。

### 4モード（AIが文脈で選ぶ）

1. **起票**（「課題にして」「載せて」／壁打ち後の合意）：
   - 壁打ち（進行フレーム）：載せる価値があるか／**高度 × 成果物**（別の高度 or 別成果物なら別ツール）／どの version・段階か。
   - 人間が「**載せて**（起票して）」と言ったら確定。
   - **起票時のチェックを通す**（→「起票時のチェック（載せる規律）」）：二つの問いのどちらかに答えられるか／`due` は支障や価値が現れる日か／`closeWhen` を書いたか。**1つでも答えられなければ載せずに聞く。**
   - **dedup の判定は「完了条件（`closeWhen`）が同じか」**（→「起票時のチェック」4）。タイトルに同じ言葉が入っていても、確かめたい状態が違えば別件。既にあるなら**新規に載せるか、既存に行動を足すか**を聞く。載せた後に気づいたら `closed.how: "duplicate"` ＋ `links` で元を指す（→ 依頼例⑥-2）。
   - **課題を1件足す**：`issues[]` の末尾に `id`（既存の最大+1）／`title`／`priority`／`opened`／`due`／`ifIgnored` **または** `ifDone`／`closeWhen`／`decisions`／`pending: null`／`closed: null`／`links`／`actions`。
   - **AI着手メモ（任意）**：着手セッションの立ち上がりを速くする**腐りにくいポインタだけ**を課題の `note`（または `_ai`）に書く —— 入口ファイル・検証コマンド・制約（触らないもの）・スコープ外。**手順のステップ列は書かない**（HOW は着手時に AI が現物のコードから組む。プロンプトは書いた瞬間から腐るが、課題と完了条件は腐らない）。
   - **GitHub Issue から転記するとき**は、**本文だけでなくコメントも必ず読む**（`gh issue view <N> --comments`）。**著者が後から足した受け入れ条件・決定・却下の理由はコメント側にあることが多い**ので、本文だけを写すと `closeWhen` が古いまま残る。コメントで決まったことは `decisions`（`decided` と `a`）に、受け入れ条件は `closeWhen` に写す。
   - **`priority` は本文に書かれている値をそのまま使う**（自分で見積もり直さない）。**`中〜高` のような範囲表記は上側を採る**（＝`high`）。本文に優先度が無ければ既定の `mid` にして、返事に「本文に優先度が無いので中にしました」と1行添える。
   - **計画への反映**：必要なら `tasks` に**葉を1つ**足し、課題の `links` に `{ "wbs": "<足した id>" }` を書く（→ 依頼例⑦-4）。`qty`／`hours`／`plan` は**利用者に聞く**（仮の数字を入れない）。
2. **着手**（「着手した」）：対応する**葉の `actual.start` に当日**（→「計画（`tasks`）の追加・更新 ①」）。葉がまだ無ければ先に⑦-4。**どの葉かを探すのは `jq`**（→「まず jq で拾う」⑤）。
   - **着手の事実を課題の `actions[]` にも書かない**（同じ事実を計画と課題の両方に持たない＝「取り扱いの原則」の最後の行）。着手したことは**計画の実績列と逆引き札**で見える。課題の `actions[]` に足すのは「打合せをした」「質問票を送った」のような**その課題の中の手順**だけ。
3. **完了**（「完了」「閉じて」）：**閉じる前の下調べ（未了の行動・未決・待ちの有無）は `jq` で拾う**（→「まず jq で拾う」②③④）。**自動検証**（"直したはず"で閉じない）→ **葉の `actual.end`（空なら `actual.start` にも当日）** ＋ **課題を `closed`**（`at`／`how`／`note`＝`closeWhen` に対応する**確かめた事実**）。`pending` が残っていれば `null` に戻す。**未了の行動や未決が残っているなら、閉じる前に食い違いを示して聞く**（→「依頼が曖昧・矛盾する時の作法」14）。
4. **エラー自動起票**（テスト/CI 失敗）：**dedup**（エラー署名で既存の未クローズ課題を検索・判定は完了条件が同じか）＋**閾値**（N回連続で初起票・flaky を弾く）→ 自動で `issues[]` に1件（再現手順／ログ／期待＝`closeWhen`）。必要なら葉＋`links` も。※ クライアント完結ツール（テレメトリ無し）は**テスト失敗のみ**。

### 完了の自動検証
機械で確認できる完了条件は**実行**する（"直したはず"で報告・クローズしない）：

- **回帰網が GREEN**：統合ビューアの e2e と、取り込み元の計画側 e2e を両方通す。
- **`uv run python scripts/check.py <正本の JSON>`** がエラー 0（番号の重複・日付・enum・リンク先の存在・旧キーの残存）。
- PIIグレップ／特定挙動。
- 人間の目が要る（**保存パスの実機スモーク**・見た目・UX）は**人間確認**。
- **このリポジトリの具体的なコマンドは末尾の「このリポの正本」節**にまとめてある。絶対パスや社内名を含む手順は公開ファイルに書かない（→ ガードレール）。

### GitHub Issue を正本にしているリポの互換手順（移行前）
**新しく始めるなら JSON の課題管理（上記）を使う。**まだ GitHub Issue を正本にしているリポでは、従来どおり次で運用してよい（移行するまでの互換経路）。

- **真実の分担**：Issue ＝ 問題・課題・完了条件（WHAT / WHY / DONE）／WBS ＝ その作業分解（HOW / WHEN）。繋ぐ鍵 ＝ `#N`。**1 issue → N 作業項目**（WBS の親ノード＝issue タイトル、子＝作業分解、親の `note` に `#N：URL`）。
- **dedup**：`gh issue list --state all --search "<keywords> in:title,body"`。あれば既存に追記。
- **Issue 作成**：背景／問題・課題／提案／論点／完了条件（受け入れ条件＝チェックボックス）／優先度／関連(#)。
- **着手**＝対応する WBS リーフの `actual.start`／**完了**＝自動検証 → issue を close（検証結果を要約コメント）＋ `actual.end`。
- **規則が食い違ったら JSON 側が正**。GitHub Issue 経路は移行前の互換であって、並び立つ2つ目の規則ではない。

### 設定（リポジトリごとに決める）
- **正本のファイル名**（課題も開発計画もここ）。
- **課題タイトルの流儀**：`【表示】【編集】` 等のカテゴリ接頭辞を付けてよい（全角30字目安）。
- **version**：後方互換の機能追加＝MINOR／バグ修正＝PATCH／破壊的・正体変更＝MAJOR（一度だけ）。例：**課題管理の追加は MAJOR（`v1.4` → `v2.0`）**＝製品の正体が変わるので「一度だけ」の枠を使う。

### ガードレール（毎回）
- **push 前 PII grep**（ユーザー名・メール・絶対パス・実名・社内名）／コミットメール＝**GitHub noreply**／`git push --force` 禁止。
- サンプル/スクショは**架空名のみ**。`.env` 等の秘密は AI が直接編集せずユーザーへ依頼。
- **実データにも社内名・端末環境名を書かない**（`wbs_roadmap.json` を含む）。正本の JSON は公開リポにコミットされるので、サンプルと同じ規律が要る。課題の `title`／`note`／`actions[].text`／`_ai` に、勤務先や顧客の名前・社内システム名・マシン名・ユーザー名・絶対パスを書かない。書く必要があるものは手元の `wbs.json`（gitignore 済み）に置く。
- **「直したはず」で報告/クローズしない**（必ず自動検証を通す）。出力は簡潔に＋**判断の理由を1行残す**。
- **迷ったら編集せず聞く**（→「依頼が曖昧・矛盾する時の作法」）。聞き返しは2〜3択にして、おすすめを1つ添える。

---

## このリポの正本

**本リポの正本＝ `wbs_roadmap.json`。**（`projects[0]` に **開発計画（`tasks`）と課題（`issues`）が同居**している。repo＝`piguo45/single-file-wbs`）

- single-file-wbs の課題・TODO・未決はすべてこのファイルの `issues[]` に載せる。開発計画（いつ・誰が・どれだけ）は同じ案件の `tasks` に載せる。**課題側に工数や日付を転記しない**（計画は計画、課題は「何を・なぜ」）。
- **GitHub Issue は新規には使わない**（正本を2か所に持たない）。2026-09-11 に、開いていた 26 件を `issues[]` へ移した。
- **Claude Code には GitHub Issue を読み取り専用にするフックを同梱**（`.claude/settings.json` ＋ `.claude/hooks/block-gh-issue-write.sh`・リポで持ち歩く）。`gh issue list/view/status` だけ通し、それ以外の `gh issue` と `gh api` の書き込みは実行前に止める。人間が書く時はプロンプトで `! gh issue …` と打つ（Claude を経由しない）。
- **外部からの報告窓口としての GitHub Issue は残る**（公開リポなので誰でも立てられる）。受け取ったら**内容を `issues[]` へ転記**し、`links` に `{ "title": "GitHub #N", "url": "…" }` を添える。以後は JSON 側で追う。
- 正本ルールは**リポ単位で宣言・1リポ1正本**。他のリポでは GitHub Issue が正本のこともある（各リポの `CLAUDE.md` の宣言に従う → 「開発運用：`/pm` スキル」の互換手順）。
- 自分の実データ `wbs.json` は gitignore 済み（正本ではない・手元の実験用）。

**このリポの完了の自動検証（`/pm` の「完了の自動検証」の具体値）**

```
uv run python tests/e2e/run_all.py         # 計画側の回帰網（golden + pixel）
uv run python tests/e2e_issue/run_all.py   # 課題側の e2e
uv run python scripts/check.py wbs_roadmap.json wbs_sample_issues.json
```

## 統合の状況（v2.0.0・完了）

**計画（WBS）と課題は、1枚の HTML・1つの JSON・1つの `CLAUDE.md` に統合済み**（2026-09-11・`v2.0.0`）。
課題管理の追加は**製品の正体が変わる変更**なので MAJOR を上げた（`v1.4` → `v2.0.0`・作者裁定）。

- データ形式と課題の描画は、姉妹リポ single-file-issue（非公開リポ）で v0.1〜v0.2 として先に固め、**本リポへ移した**。移植元リポは**設計の履歴として凍結**する（設計の一次記録は [`docs/design/brief-v0.1.md`](docs/design/brief-v0.1.md)・[`docs/design/brief-v0.2-unified.md`](docs/design/brief-v0.2-unified.md)・[`docs/design/unified-touchpoints.md`](docs/design/unified-touchpoints.md)、判断は [`docs/adr/`](docs/adr/) の 0008〜0011）。
- **課題だけの単体ビューア（`issue_viewer.html`）は退役**した。製品は `wbs_viewer.html` 1枚。
- **計画だけの `wbs.json` は今までどおり開ける**（`issues` が無ければ「計画」だけを出し、スイッチも出さない）。
- **課題番号を WBS の項番（1.1.1）に揃えることはしない**（挿入で番号がずれる・WBS に載せない課題が番号を持てない）。番号は案件内の通し番号のまま、WBS 項番は `links` から導いて隣に出す。
